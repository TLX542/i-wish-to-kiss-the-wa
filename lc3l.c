// Unified compiler + assembler for the LC-3 Language
// Usage: ./lc3l <filename>.lc3
// Outputs: <filename>.asm, <filename>_heap.obj, <filename>_code.obj

#define _POSIX_C_SOURCE 200809L // Added to fix the behavior on Linux
#include <ctype.h>
#include <errno.h>
#include <fcntl.h>
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h> // Note: strdup() was a POSIX function, not a standard C function
#include <strings.h>
#include <sys/stat.h>
#include <sys/types.h>

#include "./mpc.h"

// Compiler code

typedef struct lc3c_heap_rec {
  char *var_id;
  int value;
} lc3c_heap_rec;

typedef struct lc3c_heap_list {
  lc3c_heap_rec *heap_rec;
  struct lc3c_heap_list *next;
} lc3c_heap_list;

typedef struct lc3c_var_address {
  char *var;
  char *address;
} lc3c_var_address;

typedef struct lc3c_var_address_list {
  lc3c_var_address *var_data;
  struct lc3c_var_address_list *next;
} lc3c_var_address_list;

void heap_list_free(lc3c_heap_list *heap_list) {
  lc3c_heap_list *current = heap_list;
  lc3c_heap_list *temp = NULL;

  while (current) {
    temp = current;
    current = current->next;

    free(temp->heap_rec->var_id);
    temp->heap_rec->var_id = NULL;
    free(temp->heap_rec);
    temp->heap_rec = NULL;
    free(temp);
    temp = NULL;
  }
}

int heap_list_add(lc3c_heap_list **heap_list, const char *var_id, int value) {
  lc3c_heap_list *current = *heap_list;
  lc3c_heap_list *prev = current;
  int len = 0;

  while (current) {
    prev = current;
    current = current->next;
  }

  current = (lc3c_heap_list *)malloc(sizeof(lc3c_heap_list));
  if (!current) {
    perror("malloc");
    fprintf(stderr, "ERROR: New heap list node could not be allocated\n");
    return 0;
  }

  current->heap_rec = (lc3c_heap_rec *)malloc(sizeof(lc3c_heap_rec));
  if (!(current->heap_rec)) {
    perror("malloc");
    fprintf(stderr, "ERROR: New heap record could not be allocated\n");
    free(current);
    current = NULL;
    return 0;
  }

  len = snprintf(NULL, 0, "%s", var_id);
  if (len < 0) {
    perror("sprintf");
    fprintf(stderr, "ERROR: New heap record could not be written\n");
    free(current->heap_rec);
    current->heap_rec = NULL;
    free(current);
    current = NULL;
    return 0;
  }

  current->heap_rec->var_id = (char *)malloc((size_t)len + 1);
  if (!(current->heap_rec->var_id)) {
    perror("malloc");
    fprintf(stderr, "ERROR: New heap record could not be written\n");
    free(current->heap_rec);
    current->heap_rec= NULL;
    free(current);
    current = NULL;
    return 0;
  }

  strcpy(current->heap_rec->var_id, var_id);

  current->heap_rec->value = value;
  current->next = NULL;

  if (prev) {
    prev->next = current;
  }
  else {
    *heap_list = current;
  }

  return 1;
}

void var_address_list_free(lc3c_var_address_list *var_address_list) {
  lc3c_var_address_list *current = var_address_list;
  lc3c_var_address_list *temp = NULL;

  while (current) {
    temp = current;
    current = current->next;

    free(temp->var_data->var);
    temp->var_data->var = NULL;
    free(temp->var_data->address);
    temp->var_data->address = NULL;
    free(temp->var_data);
    temp->var_data = NULL;
    free(temp);
    temp = NULL;
  }
}

int find_var(lc3c_var_address_list *var_address_list, const char *var_id, char **result_addr, int mode) {
  if (!var_address_list) {
    return 0;
  }

  lc3c_var_address_list *current = var_address_list;

  while (current) {
    if (strcmp(current->var_data->var, var_id) == 0) {
      char *address = strdup(current->var_data->address);
      if (!address) {
        perror("strdup");
        fprintf(stderr, "ERROR: Variable address could not be retrieved for %s\n", current->var_data->var);
        return 0;
      }
      
      if (mode) { // 1
        *result_addr = address;
      }
      else { // 0
        free(address);
        address = NULL;
      }
      
      return 1;
    }

    current = current->next;
  }

  return 0;
}

int get_unique_label(const char *prefix, int *unique_label_counter, char **result) {
  int size = snprintf(NULL, 0, "%s%d", prefix, *unique_label_counter) + 1;
  if (size < 0) {
    perror("snprintf");
    fprintf(stderr, "ERROR: New label could not be generated\n");
    return 0;
  }

  char *label = (char *)malloc(size);
  if (!label) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory for the new label could not be allocated\n");
    return 0;
  }

  snprintf(label, size, "%s%d", prefix, *unique_label_counter);
  (*unique_label_counter)++;

  *result = label;

  return 1;
}

int get_var_address(lc3c_var_address_list **head_var, const char *var, int *next_address, char **result) {
  if ((!var) || (!next_address)) {
    return 0;
  }

  lc3c_var_address_list *current_var = *head_var;
  lc3c_var_address_list *prev_var = current_var;

  while (current_var) {
    if (!(current_var->var_data)) {
      fprintf(stderr, "ERROR: Variable data is corrupt\n");
      return 0;
    }

    if (strcmp(current_var->var_data->var, var) == 0) {
      char *address = strdup(current_var->var_data->address);
      if (!address) {
        perror("strdup");
        fprintf(stderr, "ERROR: Variable address could not be retrieved for %s\n", current_var->var_data->var);
        return 0;
      }

      *result = address;
      return 1;
    }

    prev_var = current_var;
    current_var = current_var->next;
  }

  lc3c_var_address_list *new_var = (lc3c_var_address_list *)malloc(sizeof(lc3c_var_address_list));
  if (!new_var) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory for the new variable list record could not be allocated\n");
    return 0;
  }

  lc3c_var_address *new_var_data = (lc3c_var_address *)malloc(sizeof(lc3c_var_address));
  if (!new_var_data) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory for the new variable record could not be allocated\n");
    free(new_var);
    new_var = NULL;
    return 0;
  }

  new_var_data->var = strdup(var);
  if (!new_var_data->var) {
    perror("strdup");
    fprintf(stderr, "ERROR: The new variable name could not be written\n");
    free(new_var_data);
    new_var_data = NULL;
    free(new_var);
    new_var = NULL;
    return 0;
  }

  int size = snprintf(NULL, 0, "x%04x", (*next_address)) + 1;
  char *new_address = (char *)malloc(size);
  if (!new_address) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory for the new variable address could not be allocated for %s\n", new_var_data->var);
    free(new_var_data->var);
    new_var_data->var = NULL;
    free(new_var_data);
    new_var_data = NULL;
    free(new_var);
    new_var = NULL;
    return 0;
  }

  snprintf(new_address, size, "x%04x", (*next_address));

  (*next_address)++;

  new_var_data->address = strdup(new_address);
  if (!new_var_data->address) {
    perror("strdup");
    fprintf(stderr, "ERROR: The new variable address could not be written for %s\n", new_var_data->var);
    free(new_address);
    new_address = NULL;
    free(new_var_data->var);
    new_var_data->var = NULL;
    free(new_var_data);
    new_var_data = NULL;
    free(new_var);
    new_var = NULL;
    return 0;
  }

  free(new_address);
  new_address = NULL;

  new_var->var_data = new_var_data;
  new_var->next = NULL;

  *result = strdup(new_var->var_data->address);
  if (!(*result)) {
    perror("strdup");
    fprintf(stderr, "ERROR: The variable address could not be retrieved for %s\n", new_var_data->var);
    free(new_var->var_data->address);
    new_var->var_data->address = NULL;
    free(new_var->var_data->var);
    new_var->var_data->var = NULL;
    free(new_var->var_data);
    new_var->var_data = NULL;
    free(new_var);
    new_var = NULL;
    return 0;
  }

  if (prev_var) {
    prev_var->next = new_var;
  } else {
    *head_var = new_var;
  }

  return 1;
}

char *strip_comments(const char *s) {
  enum {NORMAL, SLASH, LINE} state = NORMAL;
  size_t length = strlen(s);
  char *out = (char *)malloc(length + 1);
  size_t oi = 0;

  for (size_t i = 0; i < length; i++) {
    char c = s[i];

    if (state == NORMAL) {
      if (c == '/') {
        state = SLASH;
      }
      else {
        out[oi++] = c;
      }
    }
    else if (state == SLASH) {
      if (c == '/') {
        state = LINE;
      }
      else {
        out[oi++] = '/';
        out[oi++] = c;
        state = NORMAL;
      }
    }
    else if (state == LINE) {
      if (c == '\n') {
        out[oi++] = '\n';
        state = NORMAL;
      }
    }
  }

  if (state == SLASH) {
    out[oi++] = '/';
  }

  out[oi] = '\0';
  char *tmp = (char *)realloc(out, oi + 1);
  if (!tmp) {
    perror("realloc");
    fprintf(stderr, "ERROR: Memory reallocation failed while stripping the comment\n");
    free(out);
    out = NULL;
    return NULL;
  }

  out = tmp;
  return out;
}

ssize_t write_instruction(int fd, const void *buf, size_t count) {
  const uint8_t *p = (const uint8_t *)buf;
  size_t left = count;

  while (left > 0) {
    ssize_t w = write(fd, p, left);
    if (w == -1) {
      if (errno == EINTR) {
        continue;
      }

      perror("write");
      fprintf(stderr, "ERROR: An instruction could not be written to the file\n");
      return -1;
    }

    left -= (size_t)w;
    p += w;
  }

  return (ssize_t)count;
}

int floor_div(int a, int b) {
  int q = a / b;
  int r = a % b;
  if (r != 0 && ((r < 0) != (b < 0))) {
    q -= 1;
  }
  return q;
}

int compile_boolean(mpc_ast_t *bool_node, lc3c_var_address_list *var_list, int *label_counter, int write_fd) {
  mpc_ast_t *operand = bool_node->children[0];
  mpc_ast_t *op = bool_node->children[1];
  mpc_ast_t *value = bool_node->children[2];

  char *label_true = NULL;
  char *label_false = NULL;
  char *label_end = NULL;

  char *lhs_reg = NULL;
  char *rhs_reg = NULL;
  char *lhs_id = NULL;
  char *rhs_id = NULL;
  char *lhs_int = NULL;
  char *rhs_int = NULL;

  char *operand_ref = NULL;
  char *value_ref = NULL;

  char *instr = NULL;
  char *const_instr = NULL;
  
  int lhs_imm = 0;
  int rhs_imm = 0;
  int len = 0;
  int times = 0;
  int remainder = 0;

  // If both LHS and RHS are registers
  
  if ((strstr(operand->tag, "tREG|regex") != NULL) && (strstr(value->tag, "tREG|regex") != NULL)) {
    lhs_reg = strdup(operand->contents);
    if (!lhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_reg = strdup(value->contents);
    if (!rhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_reg);
      lhs_reg = NULL;

      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true); // If zero, condition is true

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;

        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;

        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false); // If zero, condition is false

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true); // If negative, condition is true

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true); // If negative or zero, condition is true

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true); // If zero or positive, condition is true

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "NOT R5, %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "NOT R5, %s\n", rhs_reg); // Bitwise complement of value is taken

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "ADD R5, R5, #1\n"; // 2's complement of value is taken
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg); // R5 = operand + R5 (- value)

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true); // If positive, condition is true

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n"; // False: Set to 0
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n"; // True: Set to 1
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
  }
  // If LHS is a register, and RHS is a variable
  
  else if ((strstr(operand->tag, "tREG|regex") != NULL) && (strstr(value->tag, "tID|regex") != NULL)) {
    lhs_reg = strdup(operand->contents);
    if (!lhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_id = strdup(value->contents);
    if (!rhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_reg);
      lhs_reg = NULL;
      
      return 0;
    }

    if (!find_var(var_list, rhs_id, &value_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, %s, R5\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, %s, R5\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If LHS is a register, and RHS is a constant
  
  else if ((strstr(operand->tag, "tREG|regex") != NULL) && (strstr(value->tag, "tINT|regex") != NULL)) {
    lhs_reg = strdup(operand->contents);
    if (!lhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_int = strdup(value->contents);
    if (!rhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_reg);
      lhs_reg = NULL;
      
      return 0;
    }

    if (sscanf(rhs_int, "%d", &rhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
      
      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }
    else if ((rhs_imm <= -4080) || (rhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", rhs_imm);
      
      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_reg);
        lhs_reg = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_reg);
      lhs_reg = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If LHS is a variable, and RHS is a register
  
  else if ((strstr(operand->tag, "tID|regex") != NULL) && (strstr(value->tag, "tREG|regex"))) {
    lhs_id = strdup(operand->contents);
    if (!lhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_reg = strdup(value->contents);
    if (!rhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_id);
      lhs_id = NULL;
      
      return 0;
    }

    if (!find_var(var_list, lhs_id, &operand_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_id);
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R6, R6, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "ADD R6, R6 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R6, R6 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If both LHS and RHS are variables
  
  else if ((strstr(operand->tag, "tID|regex") != NULL) && (strstr(value->tag, "tID|regex"))) {
    lhs_id = strdup(operand->contents);
    if (!lhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_id = strdup(value->contents);
    if (!rhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_id);
      lhs_id = NULL;
      
      return 0;
    }

    if (!find_var(var_list, lhs_id, &operand_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_id);
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }

    if (!find_var(var_list, rhs_id, &value_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "LDR R6, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R6, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R6, R6, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, R6\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If LHS is a variable, and RHS is a constant
  
  else if ((strstr(operand->tag, "tID|regex") != NULL) && (strstr(value->tag, "tINT|regex") != NULL)) {
    lhs_id = strdup(operand->contents);
    if (!lhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_int = strdup(value->contents);
    if (!rhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_id);
      lhs_id = NULL;
      
      return 0;
    }

    if (!find_var(var_list, lhs_id, &operand_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_id);
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }

    if (sscanf(rhs_int, "%d", &rhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
      
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      
      return 0;
    }
    else if ((rhs_imm <= -4080) || (rhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", rhs_imm);
      
      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
    
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
                
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", operand_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", operand_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(operand_ref);
              operand_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(operand_ref);
            operand_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_id);
        lhs_id = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(operand_ref);
        operand_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_id);
      lhs_id = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(operand_ref);
      operand_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If LHS is a constant, and RHS is a register
  
  else if ((strstr(operand->tag, "tINT|regex") != NULL) && (strstr(value->tag, "tREG|regex") != NULL)) {
    lhs_int = strdup(operand->contents);
    if (!lhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_reg = strdup(value->contents);
    if (!rhs_reg) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_int);
      lhs_int = NULL;
      
      return 0;
    }

    if (sscanf(lhs_int, "%d", &lhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_int);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      
      return 0;
    }
    else if ((lhs_imm <= -4080) || (lhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", lhs_imm);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "ADD R5, R5 %s\n", rhs_reg);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "ADD R5, R5 %s\n", rhs_reg);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_reg);
        rhs_reg = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_reg);
      rhs_reg = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If LHS is a constant, and RHS is a variable
  
  else if ((strstr(operand->tag, "tINT|regex") != NULL) && (strstr(value->tag, "tID|regex") != NULL)) {
    lhs_int = strdup(operand->contents);
    if (!lhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_id = strdup(value->contents);
    if (!rhs_id) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_int);
      lhs_int = NULL;
      
      return 0;
    }

    if (sscanf(lhs_int, "%d", &lhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_int);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }
    else if ((lhs_imm <= -4080) || (lhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", lhs_imm);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }

    if (!find_var(var_list, rhs_id, &value_ref, 1)) {
      fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;

      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "LDR R5, R7, %s\n", value_ref);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", value_ref);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "NOT R5, R5\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(value_ref);
              value_ref = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(value_ref);
            value_ref = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_id);
        rhs_id = NULL;
        free(value_ref);
        value_ref = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_id);
      rhs_id = NULL;
      free(value_ref);
      value_ref = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }
  // If both LHS and RHS are constants
  
  else if ((strstr(operand->tag, "tINT|regex") != NULL) && (strstr(value->tag, "tINT|regex") != NULL)) {
    lhs_int = strdup(operand->contents);
    if (!lhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The left-hand side operand for the Boolean expression could not be retrieved\n");
      
      return 0;
    }

    rhs_int = strdup(value->contents);
    if (!rhs_int) {
      perror("strdup");
      fprintf(stderr, "ERROR: The right-hand side operand for the Boolean expression could not be retrieved\n");
      free(lhs_int);
      lhs_int = NULL;
      
      return 0;
    }

    if (sscanf(lhs_int, "%d", &lhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_int);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }
    else if ((lhs_imm <= -4080) || (lhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", lhs_imm);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }

    if (sscanf(rhs_int, "%d", &rhs_imm) != 1) {
      fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }
    else if ((rhs_imm <= -4080) || (rhs_imm >= 3825)) {
      fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", rhs_imm);
      
      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      
      return 0;
    }

    if (strstr(op->tag, "tEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }
      
      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tNEQ") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_false)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }
      
      len = snprintf(NULL, 0, "BRz %s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRz %s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_false);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_false);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRn %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRn %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tLTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRnz %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRnz %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGTE") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRzp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRzp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
    else if (strstr(op->tag, "tGT") != NULL) {
      if ((!get_unique_label("L", label_counter, &label_true)) || (!get_unique_label("L", label_counter, &label_end))) {
        fprintf(stderr, "ERROR: Labels could not be generated\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "AND R5, R5, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return EXIT_FAILURE;
      }

      if (lhs_imm != 0) {
        if ((lhs_imm >= -16) && (lhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", lhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", lhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (lhs_imm <= -16) {
          times = floor_div(lhs_imm, -16);
          remainder = lhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(lhs_imm, 15);
          remainder = lhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      if (rhs_imm != 0) {
        if ((rhs_imm >= -16) && (rhs_imm <= 15)) {
          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", rhs_imm);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", rhs_imm);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }
          
          free(instr);
          instr = NULL;
        }
        else if (rhs_imm <= -16) {
          times = floor_div(rhs_imm, -16);
          remainder = rhs_imm - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(rhs_imm, 15);
          remainder = rhs_imm - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R5, R5, #-15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              free(lhs_int);
              lhs_int = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return 0;
            }
          }

          len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return 0;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            free(lhs_int);
            lhs_int = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return 0;
          }

          free(instr);
          instr = NULL;
        } 
      }

      len = snprintf(NULL, 0, "BRp %s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;

        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BRp %s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "BR %s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "BR %s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      len = snprintf(NULL, 0, "%s\n", label_true);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_true);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(instr);
      instr = NULL;

      const_instr = "AND R5, R5, #0\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      const_instr = "ADD R5, R5, #1\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        fprintf(stderr, "ERROR: The instruction could not be written\n");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      len = snprintf(NULL, 0, "%s\n", label_end);

      if (len < 0) {
        perror("snprintf");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }
      
      instr = (char *)malloc((size_t)len + 1);
      if (!instr) {
        perror("malloc");
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        
        return 0;
      }

      snprintf(instr, (size_t)len + 1, "%s\n", label_end);

      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
        perror("write");
        
        free(lhs_int);
        lhs_int = NULL;
        free(rhs_int);
        rhs_int = NULL;
        free(instr);
        instr = NULL;
        
        return 0;
      }

      free(lhs_int);
      lhs_int = NULL;
      free(rhs_int);
      rhs_int = NULL;
      free(instr);
      instr = NULL;
      
      return 1;
    }
  }

  return 0;
}

int compile(mpc_ast_t *ast, lc3c_var_address_list **var_list, lc3c_heap_list **heap_list, int *next_addr, int init, int *label_counter, int write_fd) {
  mpc_ast_t *ast_next = NULL;
  mpc_ast_t *root = NULL;

  char *const_instr = NULL;
  char *instr = NULL;
  char *lhs_reg = NULL;
  char *rhs_reg = NULL;
  char *lhs_id = NULL;
  char *rhs_id = NULL;

  char *rhs_int = NULL;
  
  char *lhs_term_reg = NULL;
  char *rhs_term_reg = NULL;
  char *lhs_term_id = NULL;
  char *rhs_term_id = NULL;
  char *lhs_term_int = NULL;
  char *rhs_term_int = NULL;
  
  char *var_addr = NULL;
  int len = 0;
  int imm = 0;
  int imm1 = 0;
  int imm2 = 0;
  int times = 0;
  int remainder = 0;
  int status = 0;

  char *buf = NULL;

  mpc_ast_t *lhs = NULL;
  mpc_ast_t *rhs = NULL;
  mpc_ast_t *lhs_term = NULL;
  mpc_ast_t *op = NULL;
  mpc_ast_t *rhs_term = NULL;
  
  if (init) {
    root = ast->children[1];

    // The initial instructions are written
    const_instr = ".ORIG x3000\n";
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "AND R7, R7, #0\n"; // x0
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, #1\n"; // x1
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x2
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x4
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x8
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x10
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x20
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x40
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x80
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x100
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x200
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x400
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x800
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x1000
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x2000
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    const_instr = "ADD R7, R7, R7\n"; // x4000
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      fprintf(stderr, "ERROR: The instruction could not be written\n");
         
      return EXIT_FAILURE;
    }

    if (strstr(root->tag, "brk_stmt|>") != NULL) {
      // No loop; just assign and return
      mpc_ast_t *page = ast_next->children[2];
      mpc_ast_t *write = ast_next->children[4];
      mpc_ast_t *read = ast_next->children[6];
      mpc_ast_t *alloc = ast_next->children[8];

      int vpn = 0;
      if (sscanf(page->contents, "%d", &vpn) != 1) {
        fprintf(stderr, "ERROR: Failed to parse page number: '%s'\n", page->contents);
            
        return EXIT_FAILURE;
      }

      int write_mode = 0;
      if (sscanf(write->contents, "%d", &write_mode) != 1) {
        fprintf(stderr, "ERROR: Failed to parse write mode: '%s'\n", write->contents);
            
        return EXIT_FAILURE;
      }

      int read_mode = 0;
      if (sscanf(read->contents, "%d", &read_mode) != 1) {
        fprintf(stderr, "ERROR: Failed to parse read mode: '%s'\n", read->contents);
            
        return EXIT_FAILURE;
      }

      int alloc_mode = 0;
      if (sscanf(alloc->contents, "%d", &alloc_mode) != 1) {
        fprintf(stderr, "ERROR: Failed to parse allocation/deallocation mode: '%s'\n", alloc->contents);
            
        return EXIT_FAILURE;
      }

      if ((vpn < 0) || (vpn >= 32) || (write_mode < 0) || (write_mode > 1) || (read_mode < 0) || (read_mode > 1) || (alloc_mode < 0) || (alloc_mode > 1)) {
        fprintf(stderr, "ERROR: Incorrect BRK expression: BRK(%d,%d,%d,%d)\n", vpn, write_mode, read_mode, alloc_mode);
            
        return EXIT_FAILURE;
      }

      int16_t brk_value = (vpn << 11) | (write_mode << 2) | (read_mode << 1) | alloc_mode;
      const_instr = "AND R0, R0, #0\n";

      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
            
        return EXIT_FAILURE;
      }

      if ((brk_value >= -16) && (brk_value <= 15)) {
        len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", brk_value);
        if (len < 0) {
          perror("snprintf");
                
          return EXIT_FAILURE;
        }
        
        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
                
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", brk_value);

        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
                
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }
      else if (brk_value <= -16) {
        times = floor_div(brk_value, -16);
        remainder = brk_value - (times * -16);

        for (int i = 0; i < times; i++) {
          const_instr = "ADD R0, R0, #-16\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                    
            return EXIT_FAILURE;
          }
        }

        len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", remainder);
        if (len < 0) {
          perror("snprintf");
                
          return EXIT_FAILURE;
        }
        
        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
                
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", remainder);

        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
                
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }
      else {
        times = floor_div(brk_value, 15);
        remainder = brk_value - (times * 15);

        for (int i = 0; i < times; i++) {
          const_instr = "ADD R0, R0, #15\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
            
            return EXIT_FAILURE;
          }
        }

        len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", remainder);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }
        
        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", remainder);

        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }

      const_instr = "BRK\n";
      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
        perror("write");
        
        return EXIT_FAILURE;
      }
    }
  }
  else {
    root = ast;
  }
  
  if (strstr(root->tag, "assignment|>") != NULL) {
    // No loop; just assign and proceed to return
    lhs = root->children[0];
    rhs = root->children[2];
    
    if (strstr(lhs->tag, "tID|regex") != NULL) {
      lhs_id = strdup(lhs->contents);

      if (!lhs_id) {
        perror("strdup");
        fprintf(stderr, "ERROR: The left-hand side operand could not be retrieved\n");
          
        return EXIT_FAILURE;
      }

      if (rhs->children_num == 0) {
        if (strstr(rhs->tag, "tREG|regex") != NULL) {
          // tID = tREG
          
          rhs_reg = strdup(rhs->contents);
          if (!rhs_reg) {
            perror("strdup");
            fprintf(stderr, "ERROR: The right-hand side operand could not be retrieved\n");
                      
            free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved for %s\n", lhs_id);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "STR %s, R7, %s\n", rhs_reg, var_addr);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "STR %s, R7, %s\n", rhs_reg, var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_id);
          lhs_id = NULL;
          free(rhs_reg);
          rhs_reg = NULL;
          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;
        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tID = tID
          
          rhs_id = strdup(rhs->contents);
          if (!rhs_id) {
            perror("strdup");
                      
            free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (strcmp(lhs_id, rhs_id) != 0) {
            status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
          }
        }
        else {
          // tID = tINT
          
          rhs_int = strdup(rhs->contents);
          if (!rhs_int) {
            perror("strdup");
                      
            free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, lhs_id, &buf, 0)){
            status = heap_list_add(heap_list, lhs_id, imm);

            if (!status) {
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be saved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
          } else {
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm != 0) {
              if ((imm >= -16) && (imm <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                free(instr);
                instr = NULL;
              }
              else if (imm <= -16) {
                times = floor_div(imm, -16);
                remainder = imm - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm, 15);
                remainder = imm - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }
            
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
        }
      }
      else {
        // tID = <term> (tPLUS | tMINUS) <term>
        
        lhs_term = rhs->children[0];
        op = rhs->children[1];
        rhs_term = rhs->children[2];
        
        if (strstr(op->tag, "tPLUS|regex") != NULL) {
          // tID = <term> + <term>
          

          if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tREG + tREG
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              free(instr);
              instr = NULL;
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tREG + tID
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tREG + tINT
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tID + tREG
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              free(instr);
              instr = NULL;
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              free(instr);
              instr = NULL;
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
            
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tID + tID
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

            if ((strcmp(lhs_id, lhs_term_id) == 0) && (strcmp(lhs_id, rhs_term_id) == 0)) {
              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "ADD R5, R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "ADD R5, R5, R6\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }

              const_instr = "AND R6, R6, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tID + tINT
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if ((strcmp(lhs_id, lhs_term_id) == 0) && (imm2 == 0)) {
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              return EXIT_SUCCESS;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tINT + tREG
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tINT + tID
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if ((strcmp(lhs_id, rhs_term_id) == 0) && (imm1 == 0)) {
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              return EXIT_SUCCESS;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tINT + tINT
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_id, &buf, 0)) {
              status = heap_list_add(heap_list, lhs_id, (imm1 + imm2));

              if (!status) {
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be saved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
            } else {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm1 != 0) {
                if ((imm1 >= -16) && (imm1 <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm1 <= -16) {
                  times = floor_div(imm1, -16);
                  remainder = imm1 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm1, 15);
                  remainder = imm1 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                } 
              }

              if (imm2 != 0) {
                if ((imm2 >= -16) && (imm2 <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm2 <= -16) {
                  times = floor_div(imm2, -16);
                  remainder = imm2 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm2, 15);
                  remainder = imm2 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                } 
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
        }
        else {
          // tID = <term> - <term>
          
          if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tREG - tREG
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_term_reg, rhs_term_reg) == 0) {
              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }
              
              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tREG - tID
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tREG - tINT
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                                  
              
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
            }
            
            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);
            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tID - tREG
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(instr);
            instr = NULL;
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tID - tID
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }


            if ((strcmp(lhs_id, lhs_term_id) == 0) && (strcmp(lhs_id, rhs_term_id) == 0)) {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "NOT R6, R6\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R6, R6, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, R6\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }

              const_instr = "AND R6, R6, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tID - tINT

            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if ((strcmp(lhs_id, lhs_term_id) == 0) && (imm2 == 0)) {
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              return EXIT_SUCCESS;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tID = tINT - tREG
            

            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tID = tINT - tID
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              } 
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tID = tINT - tINT
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_id, &buf, 0)) {
              status = heap_list_add(heap_list, lhs_id, (imm1 - imm2));

              if (!status) {
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be saved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
            } else {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm2 != 0) {
                if ((imm2 >= -16) && (imm2 <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm2 <= -16) {
                  times = floor_div(imm2, -16);
                  remainder = imm2 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm2, 15);
                  remainder = imm2 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              if (imm1 != 0) {
                if ((imm1 >= -16) && (imm1 <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm1 <= -16) {
                  times = floor_div(imm1, -16);
                  remainder = imm1 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm1, 15);
                  remainder = imm1 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                } 
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
        }
      }
    }
    else {
      
      lhs_reg = strdup(lhs->contents);

      if (!lhs_reg) {
        perror("strdup");
          
        return EXIT_FAILURE;
      }

      // tREG = <term>
      
      if (rhs->children_num == 0) {
        if (strstr(rhs->tag, "tINT|regex") != NULL) {
          // tREG = tINT
          
          rhs_int = strdup(rhs->contents);

          if (!rhs_int) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          
          len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          if (imm != 0) {
            if ((imm >= -16) && (imm <= 15)) {
              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if (imm <= -16) {
              times = floor_div(imm, -16);
              remainder = imm - (times * -16);

              for (int i = 0; i < times; i++) {
                len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else {
              times = floor_div(imm, 15);
              remainder = imm - (times * 15);

              for (int i = 0; i < times; i++) {
                len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
          }
        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tREG = tID
          
          rhs_id = strdup(rhs->contents);

          if (!rhs_id) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_id);
          rhs_id = NULL;
          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;
        }
        else {
          // tREG = tREG
          
          rhs_reg = strdup(rhs->contents);

          if (!rhs_reg) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (strcmp(lhs_reg, rhs_reg) == 0) {
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            return EXIT_SUCCESS;
          }

          len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }
          
          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_reg);
          rhs_reg = NULL;
          free(instr);
          instr = NULL;
        }
      }
      // tREG = <term> (tPLUS | tMINUS) <term>
      else {
        lhs_term = rhs->children[0];
        op = rhs->children[1];
        rhs_term = rhs->children[2];
        
        if (strstr(op->tag, "tPLUS|regex") != NULL) {
          // tREG = <term> + <term>
          
          if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tREG + tREG
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if ((strcmp(lhs_reg, lhs_term_reg) != 0) && (strcmp(lhs_reg, rhs_term_reg) != 0)) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(instr);
            instr = NULL;
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tREG + tID
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, lhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tREG + tINT
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
              
            if (strcmp(lhs_reg, lhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tID + tREG
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("malloc");
              free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, rhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, rhs_term_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, rhs_term_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tID + tID
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_term_id, rhs_term_id) == 0) {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tID + tINT
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                free(instr);
                instr = NULL;
              }
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tINT + tREG
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, rhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_reg);
            rhs_term_reg = NULL;
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tINT + tID
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                free(instr);
                instr = NULL;
              }
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tINT + tINT
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
            }

            if (imm2 != 0) {
              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
          }
        }
        else {
          // tREG = <term> - <term>
          
          if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tREG - tREG
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }


            if ((strcmp(lhs_reg, lhs_term_reg) == 0) && (strcmp(lhs_reg, rhs_term_reg) == 0)) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if ((strcmp(lhs_reg, lhs_term_reg) == 0) && (strcmp(lhs_reg, rhs_term_reg) != 0)) {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = NULL;

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
            }
            else if ((strcmp(lhs_reg, lhs_term_reg) != 0) && (strcmp(lhs_reg, rhs_term_reg) == 0)) {
              len = snprintf(NULL, 0, "NOT %s, %s\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }
              
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }

              const_instr = NULL;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tREG - tID
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, lhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_reg);
            lhs_term_reg = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tREG - tINT
            
            lhs_term_reg = strdup(lhs_term->contents);
            if (!lhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, lhs_term_reg) != 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            if (imm2 != 0) {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                                    
                
                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_reg);
              lhs_term_reg = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tID - tREG
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, rhs_term_reg) == 0) {
              len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s #1\n", rhs_term_reg, rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
            else {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tID - tID
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_term_id, rhs_term_id) == 0) {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tID - tINT
            
            lhs_term_id = strdup(lhs_term->contents);
            if (!lhs_term_id) {
              perror("strdup");
              free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

            if (len < 0) {
              perror("snprintf");
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_id);
              lhs_term_id = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            if (imm2 != 0) {
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              if ((imm2 >= -16) && (imm2 <= 15)) {
                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm2 <= -16) {
                times = floor_div(imm2, -16);
                remainder = imm2 - (times * -16);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #-16\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm2, 15);
                remainder = imm2 - (times * 15);

                for (int i = 0; i < times; i++) {
                  const_instr = "ADD R5, R5, #15\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_id);
            lhs_term_id = NULL;
            free(rhs_term_int);
            rhs_term_int = NULL;
            free(instr);
            instr = NULL;
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
            // tREG = tINT - tREG
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_reg = strdup(rhs_term->contents);
            if (!rhs_term_reg) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              
              return EXIT_FAILURE;
            }

            if (strcmp(lhs_reg, rhs_term_reg) == 0) {
              len = snprintf(NULL, 0, "NOT %s, %s\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              if (imm1 != 0) {
                if ((imm1 >= -16) && (imm1 <= 15)) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm1 <= -16) {
                  times = floor_div(imm1, -16);
                  remainder = imm1 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm1, 15);
                  remainder = imm1 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
            }
            else {
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              if (imm1 != 0) {
                if ((imm1 >= -16) && (imm1 <= 15)) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm1 <= -16) {
                  times = floor_div(imm1, -16);
                  remainder = imm1 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm1, 15);
                  remainder = imm1 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
              }

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_reg);
              rhs_term_reg = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                              
                return EXIT_FAILURE;
              }
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
            // tREG = tINT - tID
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_id = strdup(rhs_term->contents);
            if (!rhs_term_id) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
              fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                        
              free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            if (imm1 != 0) {
              if ((imm1 >= -16) && (imm1 <= 15)) {
                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else if (imm1 <= -16) {
                times = floor_div(imm1, -16);
                remainder = imm1 - (times * -16);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
              else {
                times = floor_div(imm1, 15);
                remainder = imm1 - (times * 15);

                for (int i = 0; i < times; i++) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }
            }
            
            status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_id);
              rhs_term_id = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(lhs_term_int);
            lhs_term_int = NULL;
            free(rhs_term_id);
            rhs_term_id = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
            // tREG = tINT - tINT
            
            lhs_term_int = strdup(lhs_term->contents);
            if (!lhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            rhs_term_int = strdup(rhs_term->contents);
            if (!rhs_term_int) {
              perror("strdup");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm1 <= -4080) || (imm1 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
              fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            else if ((imm2 <= -4080) || (imm2 >= 3825)) {
              fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(lhs_term_int);
              lhs_term_int = NULL;
              free(rhs_term_int);
              rhs_term_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if (imm1 != imm2) {
              if (imm1 != 0) {
                if ((imm1 >= -16) && (imm1 <= 15)) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                  if (len < 0) {
                    perror("snprintf");
                      
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm1 <= -16) {
                  times = floor_div(imm1, -16);
                  remainder = imm1 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm1, 15);
                  remainder = imm1 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
              }

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm2 != 0) {
                if ((imm2 >= -16) && (imm2 <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm2 <= -16) {
                  times = floor_div(imm2, -16);
                  remainder = imm2 - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm2, 15);
                  remainder = imm2 - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                          
                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  const_instr = NULL;

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                      
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  
                  
                  return EXIT_FAILURE;
                }
              }
            }
          }
        }
      }
    }
  }
  else if (strstr(root->tag, "selfarithmetic|>") != NULL) {
    // No loop; just assign and proceed to return
    lhs = root->children[0];
    op = root->children[1];
    rhs = root->children[2];
    
    
    if (strstr(lhs->tag, "tREG|regex") != NULL) {
      lhs_reg = strdup(lhs->contents);
      if (!lhs_reg) {
        perror("strdup");
          
        return EXIT_FAILURE;
      }
      
      if (strstr(op->tag, "tPLUSASSIGN|regex") != NULL) {
        // tREG += <term>
        
        if (strstr(rhs->tag, "tREG|regex") != NULL) {
          // tREG += tREG
          
          rhs_reg = strdup(rhs->contents);
          if (!rhs_reg) {
            perror("strdup");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);
          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_reg);
          rhs_reg = NULL;
          free(instr);
          instr = NULL;

        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tREG += tID
          
          rhs_id = strdup(rhs->contents);
          if (!rhs_id) {
            perror("strdup");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;

          len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_id);
          rhs_id = NULL;
          free(instr);
          instr = NULL;

          const_instr = "AND R5, R5, #0\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      
            
            return EXIT_FAILURE;
          }
        }
        else {
          // tREG += tINT
          
          rhs_int = strdup(rhs_term->contents);
          if (!rhs_int) {
            perror("strdup");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }
          
          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          if (imm != 0) {
            if ((imm >= -16) && (imm <= 15)) {
              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if (imm <= -16) {
              times = floor_div(imm, -16);
              remainder = imm - (times * -16);

              for (int i = 0; i < times; i++) {
                len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else {
              times = floor_div(imm, 15);
              remainder = imm - (times * 15);

              for (int i = 0; i < times; i++) {
                len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
              if (len < 0) {
                perror("snprintf");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_int);
          rhs_int = NULL;
        }
      }
      else {
        // tREG -= <term>
        
        if (strstr(rhs->tag, "tREG|regex") != NULL) {
          // tREG -= tREG
          
          rhs_reg = strdup(rhs->contents);
          if (!rhs_reg) {
            perror("strdup");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (strcmp(lhs_reg, rhs_reg) == 0) {
            len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
          }
          else {
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tREG -= tID
          
          rhs_id = strdup(rhs->contents);
          if (!rhs_id) {
            perror("strdup");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;

          const_instr = "NOT R5, R5\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          const_instr = "ADD R5, R5, #1\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }
          
          len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_id);
          rhs_id = NULL;
          free(instr);
          instr = NULL;
          
          const_instr = "AND R5, R5, #0\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      return EXIT_FAILURE;
          }
        }
        else {
          // tREG -= tINT
          
          rhs_int = strdup(rhs_term->contents);
          if (!rhs_int) {
            perror("strdup");
            free(lhs_reg);
            lhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
            free(lhs_reg);
            lhs_reg = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          if (imm != 0) {
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                                  
              
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            if ((imm >= -16) && (imm <= 15)) {
              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if (imm <= -16) {
              times = floor_div(imm, -16);
              remainder = imm - (times * -16);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #-16\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else {
              times = floor_div(imm, 15);
              remainder = imm - (times * 15);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #15\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

            }

            const_instr = "NOT R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
          }

          free(lhs_reg);
          lhs_reg = NULL;
          free(rhs_int);
          rhs_int = NULL;
        }
      }
    }
    else {
      
      lhs_id = strdup(lhs->contents);
      if (!lhs_id) {
        perror("strdup");
              
        return EXIT_FAILURE;
      }

      if (!find_var(*var_list, lhs_id, &buf, 0)) {
        fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_id);
                  
        free(lhs_id);
        lhs_id = NULL;
        
        return EXIT_FAILURE;
      }

      if (strstr(op->tag, "tPLUSASSIGN|regex") != NULL) {
        // tID += <term>
        
        if (strstr(rhs->tag, "tREG|regex") != NULL) {
          // tID += tREG
          
          rhs_reg = strdup(rhs->contents);
          if (!rhs_reg) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
          
          len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_id);
          lhs_id = NULL;
          free(rhs_reg);
          rhs_reg = NULL;
          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;

          const_instr = "AND R5, R5, #0\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      return EXIT_FAILURE;
          }
        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tID += tID
          
          rhs_id = strdup(rhs->contents);
          if (!rhs_id) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (strcmp(lhs_id, rhs_id) == 0) {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            const_instr = "ADD R5, R5, R5\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
          else {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;
            
            status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            const_instr = "ADD R5, R5, R6\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }

            const_instr = "AND R6, R6, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
        }
        else {
          // tID += tINT
          
          rhs_int = strdup(rhs_term->contents);
          if (!rhs_int) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          if (imm != 0) {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if ((imm >= -16) && (imm <= 15)) {
              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if (imm <= -16) {
              times = floor_div(imm, -16);
              remainder = imm - (times * -16);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #-16\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else {
              times = floor_div(imm, 15);
              remainder = imm - (times * 15);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #15\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
          }

          free(lhs_id);
          lhs_id = NULL;
          free(rhs_int);
          rhs_int = NULL;
        }
      }
      else {
        // tID -= <term>
        
        if (strstr(rhs->tag, "tREG|regex") != NULL) {
          // tID -= tREG
          
          rhs_reg = strdup(rhs->contents);
          if (!rhs_reg) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

          if (!status) {
            fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_reg, rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
          
          len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_reg, rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
          
          len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          const_instr = "AND R5, R5, #0\n";

          if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_reg, rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;

          len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_reg, rhs_reg);

          if (len < 0) {
            perror("snprintf");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_reg, rhs_reg);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_reg);
            rhs_reg = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(lhs_id);
          lhs_id = NULL;
          free(rhs_reg);
          rhs_reg = NULL;
          free(var_addr);
          var_addr = NULL;
          free(instr);
          instr = NULL;
        }
        else if (strstr(rhs->tag, "tID|regex") != NULL) {
          // tID -= tID
          
          rhs_id = strdup(rhs->contents);
          if (!rhs_id) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, rhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                      
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (strcmp(lhs_id, rhs_id) == 0) {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }
          
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
          }
          else {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "NOT R6, R6\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R6, R6, #1\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            const_instr = "ADD R5, R5, R6\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }
          
            free(lhs_id);
            lhs_id = NULL;
            free(rhs_id);
            rhs_id = NULL;
            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;

            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }

            const_instr = "AND R6, R6, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              return EXIT_FAILURE;
            }
          }
        }
        else {
          // tID -= tINT
          
          rhs_int = strdup(rhs_term->contents);
          if (!rhs_int) {
            perror("strdup");
                      free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (sscanf(rhs_int, "%d", &imm) != 1) {
            fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }
          else if ((imm <= -4080) || (imm >= 3825)) {
            fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                      free(lhs_id);
            lhs_id = NULL;
            free(rhs_int);
            rhs_int = NULL;
            
            return EXIT_FAILURE;
          }

          if (imm != 0) {
            status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

            if (!status) {
              fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }

            len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(instr);
            instr = NULL;

            if ((imm >= -16) && (imm <= 15)) {
              len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", imm);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", imm);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else if (imm <= -16) {
              times = floor_div(imm, -16);
              remainder = imm - (times * -16);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #16\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }
            else {
              times = floor_div(imm, 15);
              remainder = imm - (times * 15);

              for (int i = 0; i < times; i++) {
                const_instr = "ADD R5, R5, #-15\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
              if (len < 0) {
                perror("snprintf");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
            }

            len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

            if (len < 0) {
              perror("snprintf");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }
            
            instr = (char *)malloc((size_t)len + 1);
            if (!instr) {
              perror("malloc");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              
              return EXIT_FAILURE;
            }

            snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

            if (write_instruction(write_fd, instr, (size_t)len) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
              
              return EXIT_FAILURE;
            }

            free(var_addr);
            var_addr = NULL;
            free(instr);
            instr = NULL;
            
            const_instr = "AND R5, R5, #0\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                          
              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
              
              return EXIT_FAILURE;
            }
          }

          free(lhs_id);
          lhs_id = NULL;
          free(rhs_int);
          rhs_int = NULL;
        }
      }
    }
  }
  else if (strstr(root->tag, "yield_stmt|>") != NULL) {
    // No loop; just assign and proceed to return
    const_instr = "YIELD\n";
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      
      return EXIT_FAILURE;
    }

    const_instr = NULL;
  }
  else if (strstr(root->tag, "unmatched_if|>") != NULL) {
    // No loop; just assign and proceed to return
    // Get the Boolean expression
    mpc_ast_t *bool_node = root->children[2];
    // Get the body of the if clause
    mpc_ast_t *then_node = root->children[5];
    
    // Generate the labels
    char *cond_label = NULL;
    char *end_label = NULL;

    if ((!get_unique_label("L", label_counter, &cond_label)) || (!get_unique_label("L", label_counter, &end_label))) {
      fprintf(stderr, "ERROR: Labels could not be generated\n");
      
      return EXIT_FAILURE;
    }

    // Emit the condition label
    len = snprintf(NULL, 0, "%s\n", cond_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", cond_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the Boolean expression
    if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
      fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
      
      return EXIT_FAILURE;
    }

    // If the condition is false, branch to the end label
    len = snprintf(NULL, 0, "BRz %s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "BRz %s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the body of the if clause
    status = compile(then_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);
    if (status == EXIT_FAILURE) {
      fprintf(stderr, "ERROR: Compilation error inside then-body of unmatched_if\n");
      
      return EXIT_FAILURE;
    }

    // Emit the end label
    len = snprintf(NULL, 0, "%s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;
  }
  else if (strstr(root->tag, "matched_if|>") != NULL) {
    // No loop; just assign and proceed to return
    // Get the Boolean expression
    mpc_ast_t *bool_node = root->children[2];
    // Get the body of the if clause
    mpc_ast_t *then_node = root->children[5];
    // Get the body of the else clause
    mpc_ast_t *else_node = root->children[8];

    // Generate the labels
    char *cond_label = NULL;
    char *else_label = NULL;
    char *end_label = NULL;

    if ((!get_unique_label("L", label_counter, &cond_label)) || (!get_unique_label("L", label_counter, &else_label)) || (!get_unique_label("L", label_counter, &end_label))) {
      fprintf(stderr, "ERROR: Labels could not be generated\n");
      
      return EXIT_FAILURE;
    }

    // Emit the condition label
    len = snprintf(NULL, 0, "%s\n", cond_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", cond_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the Boolean expression
    if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
      fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
      
      return EXIT_FAILURE;
    }

    // If the condition is false, branch to the else label
    len = snprintf(NULL, 0, "BRz %s\n", else_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "BRz %s\n", else_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the body of the if clause
    status = compile(then_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);

    if (status == EXIT_FAILURE) {
      fprintf(stderr, "ERROR: Compilation error inside then-body of matched_if\n");
      
      return EXIT_FAILURE;
    }

    // Finish the if clause by jumping to the end label
    len = snprintf(NULL, 0, "BR %s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "BR %s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Emit the else label
    len = snprintf(NULL, 0, "%s\n", else_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", else_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the body of the else clause
    status = compile(else_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);
    if (status == EXIT_FAILURE) {
      fprintf(stderr, "ERROR: Compilation error inside else-body of matched_if\n");
      
      return EXIT_FAILURE;
    }

    // Emit the end label
    len = snprintf(NULL, 0, "%s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;
  }
  else if (strstr(root->tag, "while_stmt|>") != NULL) {
    // No loop; just assign and proceed to return
    // Get the Boolean expression
    mpc_ast_t *bool_node = root->children[2];
    // Get the body
    mpc_ast_t *body_node = root->children[5];
    
    // Generate the labels
    char *start_label = NULL;
    char *end_label = NULL;

    if ((!get_unique_label("L", label_counter, &start_label)) || (!get_unique_label("L", label_counter, &end_label))) {
      fprintf(stderr, "ERROR: Labels could not be generated\n");
      
      return EXIT_FAILURE;
    }

    // Emit the start label
    len = snprintf(NULL, 0, "%s\n", start_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", start_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Compile the Boolean expression
    if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
      fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
      
      return EXIT_FAILURE;
    }

    // If the condition is false, branch to the end label
    len = snprintf(NULL, 0, "BRz %s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "BRz %s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // Recursive call for the body of the loop
    status = compile(body_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);

    if (status == EXIT_FAILURE) {
      fprintf(stderr, "ERROR: Compilation error inside while loop\n");
      
      return EXIT_FAILURE;
    }

    // The loop body is compiled

    // Jump back to the start label
    len = snprintf(NULL, 0, "BR %s\n", start_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "BR %s\n", start_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;

    // End label
    len = snprintf(NULL, 0, "%s\n", end_label);
    if (len < 0) {
      perror("snprintf");
      
      return EXIT_FAILURE;
    }

    instr = (char *)malloc((size_t)len + 1);
    if (!instr) {
      perror("malloc");
      
      return EXIT_FAILURE;
    }

    snprintf(instr, (size_t)len + 1, "%s\n", end_label);
    
    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
      perror("write");
      
      free(instr);
      instr = NULL;
      
      return EXIT_FAILURE;
    }

    free(instr);
    instr = NULL;
  }
  else {
    // Loop over children
    for (int i = 0; i < root->children_num; i++) {
      ast_next = root->children[i];
      
      if (strstr(ast_next->tag, "assignment|>") != NULL) {
        lhs = ast_next->children[0];
        rhs = ast_next->children[2];
        
        if (strstr(lhs->tag, "tID|regex") != NULL) {
          
          lhs_id = strdup(lhs->contents);

          if (!lhs_id) {
            perror("strdup");
            fprintf(stderr, "ERROR: The left-hand side operand could not be retrieved\n");
              
            return EXIT_FAILURE;
          }

          if (rhs->children_num == 0) {
            if (strstr(rhs->tag, "tREG|regex") != NULL) {
              // tID = tREG
              
              rhs_reg = strdup(rhs->contents);
              if (!rhs_reg) {
                perror("strdup");
                fprintf(stderr, "ERROR: The right-hand side operand could not be retrieved\n");
                          
                free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved for %s\n", lhs_id);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "STR %s, R7, %s\n", rhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR %s, R7, %s\n", rhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tID = tID
              
              rhs_id = strdup(rhs->contents);
              if (!rhs_id) {
                perror("strdup");
                          
                free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (strcmp(lhs_id, rhs_id) != 0) {
                status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
              }
            }
            else {
              // tID = tINT
              
              rhs_int = strdup(rhs->contents);
              if (!rhs_int) {
                perror("strdup");
                          
                free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, lhs_id, &buf, 0)){
                status = heap_list_add(heap_list, lhs_id, imm);

                if (!status) {
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be saved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              } else {
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm != 0) {
                  if ((imm >= -16) && (imm <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    free(instr);
                    instr = NULL;
                  }
                  else if (imm <= -16) {
                    times = floor_div(imm, -16);
                    remainder = imm - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(rhs_int);
                        rhs_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm, 15);
                    remainder = imm - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(rhs_int);
                        rhs_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
            }
          }
          else {
            // tID = <term> (tPLUS | tMINUS) <term>
            
            lhs_term = rhs->children[0];
            op = rhs->children[1];
            rhs_term = rhs->children[2];
            
            if (strstr(op->tag, "tPLUS|regex") != NULL) {
              // tID = <term> + <term>
              

              if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tREG + tREG
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  free(instr);
                  instr = NULL;
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tREG + tID
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tREG + tINT
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tID + tREG
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  free(instr);
                  instr = NULL;
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  free(instr);
                  instr = NULL;
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
                
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tID + tID
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((strcmp(lhs_id, lhs_term_id) == 0) && (strcmp(lhs_id, rhs_term_id) == 0)) {
                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "ADD R5, R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "ADD R5, R5, R6\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }

                  const_instr = "AND R6, R6, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tID + tINT
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((strcmp(lhs_id, lhs_term_id) == 0) && (imm2 == 0)) {
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  continue;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tINT + tREG
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tINT + tID
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((strcmp(lhs_id, rhs_term_id) == 0) && (imm1 == 0)) {
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  continue;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tINT + tINT
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_id, &buf, 0)) {
                  status = heap_list_add(heap_list, lhs_id, (imm1 + imm2));

                  if (!status) {
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be saved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                } else {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  if (imm1 != 0) {
                    if ((imm1 >= -16) && (imm1 <= 15)) {
                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm1 <= -16) {
                      times = floor_div(imm1, -16);
                      remainder = imm1 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #-16\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm1, 15);
                      remainder = imm1 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #15\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    } 
                  }

                  if (imm2 != 0) {
                    if ((imm2 >= -16) && (imm2 <= 15)) {
                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm2 <= -16) {
                      times = floor_div(imm2, -16);
                      remainder = imm2 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #-16\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm2, 15);
                      remainder = imm2 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #15\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    } 
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
            }
            else {
              // tID = <term> - <term>
              
              if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tREG - tREG
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_term_reg, rhs_term_reg) == 0) {
                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tREG - tID
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tREG - tINT
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                      
                  
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }
                
                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", lhs_term_reg);
                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tID - tREG
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_term_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tID - tID
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }


                if ((strcmp(lhs_id, lhs_term_id) == 0) && (strcmp(lhs_id, rhs_term_id) == 0)) {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "NOT R6, R6\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R6, R6, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, R6\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }

                  const_instr = "AND R6, R6, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tID - tINT
                

                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((strcmp(lhs_id, lhs_term_id) == 0) && (imm2 == 0)) {
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  continue;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tID = tINT - tREG
                

                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tID = tINT - tID
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  } 
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tID = tINT - tINT
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_id, &buf, 0)) {
                  status = heap_list_add(heap_list, lhs_id, (imm1 - imm2));

                  if (!status) {
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be saved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                } else {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  if (imm2 != 0) {
                    if ((imm2 >= -16) && (imm2 <= 15)) {
                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm2 <= -16) {
                      times = floor_div(imm2, -16);
                      remainder = imm2 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #-16\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm2, 15);
                      remainder = imm2 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #15\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    const_instr = "NOT R5, R5\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    const_instr = "ADD R5, R5, #1\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      
                      free(lhs_id);
                      lhs_id = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  if (imm1 != 0) {
                    if ((imm1 >= -16) && (imm1 <= 15)) {
                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm1);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm1);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm1 <= -16) {
                      times = floor_div(imm1, -16);
                      remainder = imm1 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #-16\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm1, 15);
                      remainder = imm1 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #15\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_id);
                          lhs_id = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_id);
                        lhs_id = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    } 
                  }

                  status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_id);
                    lhs_id = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_id);
                  lhs_id = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
            }
          }
        }
        else {
          
          lhs_reg = strdup(lhs->contents);

          if (!lhs_reg) {
            perror("strdup");
              
            return EXIT_FAILURE;
          }

          // tREG = <term>
          
          if (rhs->children_num == 0) {
            if (strstr(rhs->tag, "tINT|regex") != NULL) {
              // tREG = tINT
              
              rhs_int = strdup(rhs->contents);

              if (!rhs_int) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              if (imm != 0) {
                if ((imm >= -16) && (imm <= 15)) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm <= -16) {
                  times = floor_div(imm, -16);
                  remainder = imm - (times * -16);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm, 15);
                  remainder = imm - (times * 15);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
              }
            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tREG = tID
              
              rhs_id = strdup(rhs->contents);

              if (!rhs_id) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else {
              // tREG = tREG
              
              rhs_reg = strdup(rhs->contents);

              if (!rhs_reg) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (strcmp(lhs_reg, rhs_reg) == 0) {
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                continue;
              }

              len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }
              
              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(instr);
              instr = NULL;
            }
          }
          // tREG = <term> (tPLUS | tMINUS) <term>
          else {
            lhs_term = rhs->children[0];
            op = rhs->children[1];
            rhs_term = rhs->children[2];
            
            if (strstr(op->tag, "tPLUS|regex") != NULL) {
              // tREG = <term> + <term>
              
              if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tREG + tREG
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((strcmp(lhs_reg, lhs_term_reg) != 0) && (strcmp(lhs_reg, rhs_term_reg) != 0)) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tREG + tID
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, lhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tREG + tINT
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                  
                if (strcmp(lhs_reg, lhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_reg);
                    rhs_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tID + tREG
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, rhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, rhs_term_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, rhs_term_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tID + tID
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_term_id, rhs_term_id) == 0) {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_reg);
                    rhs_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tID + tINT
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    free(instr);
                    instr = NULL;
                  }
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tINT + tREG
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, rhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_reg);
                      rhs_term_reg = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_reg);
                rhs_term_reg = NULL;
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tINT + tID
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    free(instr);
                    instr = NULL;
                  }
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tINT + tINT
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                }

                if (imm2 != 0) {
                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
              }
            }
            else {
              // tREG = <term> - <term>
              
              if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tREG - tREG
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }


                if ((strcmp(lhs_reg, lhs_term_reg) == 0) && (strcmp(lhs_reg, rhs_term_reg) == 0)) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if ((strcmp(lhs_reg, lhs_term_reg) == 0) && (strcmp(lhs_reg, rhs_term_reg) != 0)) {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = NULL;

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                }
                else if ((strcmp(lhs_reg, lhs_term_reg) != 0) && (strcmp(lhs_reg, rhs_term_reg) == 0)) {
                  len = snprintf(NULL, 0, "NOT %s, %s\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_reg);
                    rhs_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_term_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }

                  const_instr = NULL;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tREG - tID
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, lhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_reg);
                lhs_term_reg = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tREG|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tREG - tINT
                
                lhs_term_reg = strdup(lhs_term->contents);
                if (!lhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, lhs_term_reg) != 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, lhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                if (imm2 != 0) {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                        
                    
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_reg);
                        lhs_term_reg = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_reg);
                      lhs_term_reg = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_reg);
                    lhs_term_reg = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_reg);
                  lhs_term_reg = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tID - tREG
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, rhs_term_reg) == 0) {
                  len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_term_reg, rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s #1\n", rhs_term_reg, rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
                else {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tID - tID
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_term_id, rhs_term_id) == 0) {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                }
                else {
                  status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                  if (!status) {
                    fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_id);
                    rhs_term_id = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tID|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tID - tINT
                
                lhs_term_id = strdup(lhs_term->contents);
                if (!lhs_term_id) {
                  perror("strdup");
                  free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, lhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                if (len < 0) {
                  perror("snprintf");
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR %s, R7, %s\n", lhs_reg, var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_id);
                  lhs_term_id = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                if (imm2 != 0) {
                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  if ((imm2 >= -16) && (imm2 <= 15)) {
                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm2 <= -16) {
                    times = floor_div(imm2, -16);
                    remainder = imm2 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #-16\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm2, 15);
                    remainder = imm2 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      const_instr = "ADD R5, R5, #15\n";

                      if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_id);
                        lhs_term_id = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                    }

                    len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_id);
                      lhs_term_id = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_id);
                    lhs_term_id = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_id);
                lhs_term_id = NULL;
                free(rhs_term_int);
                rhs_term_int = NULL;
                free(instr);
                instr = NULL;
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tREG|regex") != NULL)) {
                // tREG = tINT - tREG
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_reg = strdup(rhs_term->contents);
                if (!rhs_term_reg) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (strcmp(lhs_reg, rhs_term_reg) == 0) {
                  len = snprintf(NULL, 0, "NOT %s, %s\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  if (imm1 != 0) {
                    if ((imm1 >= -16) && (imm1 <= 15)) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm1 <= -16) {
                      times = floor_div(imm1, -16);
                      remainder = imm1 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm1, 15);
                      remainder = imm1 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                }
                else {
                  len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  if (imm1 != 0) {
                    if ((imm1 >= -16) && (imm1 <= 15)) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm1 <= -16) {
                      times = floor_div(imm1, -16);
                      remainder = imm1 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm1, 15);
                      remainder = imm1 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_reg);
                          rhs_term_reg = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_reg);
                        rhs_term_reg = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                  }

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_term_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                  const_instr = "NOT R5, R5\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  const_instr = "ADD R5, R5, #1\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_reg);
                    rhs_term_reg = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_reg);
                  rhs_term_reg = NULL;
                  free(instr);
                  instr = NULL;

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                                  
                    return EXIT_FAILURE;
                  }
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tID|regex") != NULL)) {
                // tREG = tINT - tID
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_id = strdup(rhs_term->contents);
                if (!rhs_term_id) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (!find_var(*var_list, rhs_term_id, &buf, 0)) {
                  fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_term_id);
                            
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (imm1 != 0) {
                  if ((imm1 >= -16) && (imm1 <= 15)) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else if (imm1 <= -16) {
                    times = floor_div(imm1, -16);
                    remainder = imm1 - (times * -16);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                  else {
                    times = floor_div(imm1, 15);
                    remainder = imm1 - (times * 15);

                    for (int i = 0; i < times; i++) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_id);
                        rhs_term_id = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_id);
                      rhs_term_id = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }
                }
                
                status = get_var_address(var_list, rhs_term_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_id);
                  rhs_term_id = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(lhs_term_int);
                lhs_term_int = NULL;
                free(rhs_term_id);
                rhs_term_id = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else if ((strstr(lhs_term->tag, "tINT|regex") != NULL) && (strstr(rhs_term->tag, "tINT|regex") != NULL)) {
                // tREG = tINT - tINT
                
                lhs_term_int = strdup(lhs_term->contents);
                if (!lhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                rhs_term_int = strdup(rhs_term->contents);
                if (!rhs_term_int) {
                  perror("strdup");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(lhs_term_int, "%d", &imm1) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", lhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm1 <= -4080) || (imm1 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm1);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if (sscanf(rhs_term_int, "%d", &imm2) != 1) {
                  fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_term_int);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                else if ((imm2 <= -4080) || (imm2 >= 3825)) {
                  fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm2);
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(lhs_term_int);
                  lhs_term_int = NULL;
                  free(rhs_term_int);
                  rhs_term_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if (imm1 != imm2) {
                  if (imm1 != 0) {
                    if ((imm1 >= -16) && (imm1 <= 15)) {
                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);
                      if (len < 0) {
                        perror("snprintf");
                          
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm1);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm1 <= -16) {
                      times = floor_div(imm1, -16);
                      remainder = imm1 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm1, 15);
                      remainder = imm1 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                        if (len < 0) {
                          perror("snprintf");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                        
                        instr = (char *)malloc((size_t)len + 1);
                        if (!instr) {
                          perror("malloc");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          free(instr);
                          instr = NULL;
                          
                          return EXIT_FAILURE;
                        }

                        free(instr);
                        instr = NULL;
                      }

                      len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                  }

                  const_instr = "AND R5, R5, #0\n";

                  if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                    perror("write");
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  if (imm2 != 0) {
                    if ((imm2 >= -16) && (imm2 <= 15)) {
                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm2);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm2);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else if (imm2 <= -16) {
                      times = floor_div(imm2, -16);
                      remainder = imm2 - (times * -16);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #-16\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }
                    else {
                      times = floor_div(imm2, 15);
                      remainder = imm2 - (times * 15);

                      for (int i = 0; i < times; i++) {
                        const_instr = "ADD R5, R5, #15\n";

                        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                          perror("write");
                                              
                          
                          free(lhs_reg);
                          lhs_reg = NULL;
                          free(lhs_term_int);
                          lhs_term_int = NULL;
                          free(rhs_term_int);
                          rhs_term_int = NULL;
                          
                          return EXIT_FAILURE;
                        }
                      }

                      const_instr = NULL;

                      len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                      if (len < 0) {
                        perror("snprintf");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }
                      
                      instr = (char *)malloc((size_t)len + 1);
                      if (!instr) {
                        perror("malloc");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                      if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                        perror("write");
                                          
                        
                        free(lhs_reg);
                        lhs_reg = NULL;
                        free(lhs_term_int);
                        lhs_term_int = NULL;
                        free(rhs_term_int);
                        rhs_term_int = NULL;
                        free(instr);
                        instr = NULL;
                        
                        return EXIT_FAILURE;
                      }

                      free(instr);
                      instr = NULL;
                    }

                    const_instr = "NOT R5, R5\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    const_instr = "ADD R5, R5, #1\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(lhs_term_int);
                      lhs_term_int = NULL;
                      free(rhs_term_int);
                      rhs_term_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(lhs_term_int);
                    lhs_term_int = NULL;
                    free(rhs_term_int);
                    rhs_term_int = NULL;
                    free(instr);
                    instr = NULL;

                    const_instr = "AND R5, R5, #0\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      
                      
                      return EXIT_FAILURE;
                    }
                  }
                }
              }
            }
          }
        }
      }
      
      else if (strstr(ast_next->tag, "selfarithmetic|>") != NULL) {
        lhs = ast_next->children[0];
        op = ast_next->children[1];
        rhs = ast_next->children[2];
        
        
        if (strstr(lhs->tag, "tREG|regex") != NULL) {
          lhs_reg = strdup(lhs->contents);
          if (!lhs_reg) {
            perror("strdup");
              
            return EXIT_FAILURE;
          }
          
          if (strstr(op->tag, "tPLUSASSIGN|regex") != NULL) {
            // tREG += <term>
            
            if (strstr(rhs->tag, "tREG|regex") != NULL) {
              // tREG += tREG
              
              rhs_reg = strdup(rhs->contents);
              if (!rhs_reg) {
                perror("strdup");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);
              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, %s\n", lhs_reg, lhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(instr);
              instr = NULL;

            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tREG += tID
              
              rhs_id = strdup(rhs->contents);
              if (!rhs_id) {
                perror("strdup");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          
                
                return EXIT_FAILURE;
              }
            }
            else {
              // tREG += tINT
              
              rhs_int = strdup(rhs->contents);
              if (!rhs_int) {
                perror("strdup");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm != 0) {
                if ((imm >= -16) && (imm <= 15)) {
                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, imm);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm <= -16) {
                  times = floor_div(imm, -16);
                  remainder = imm - (times * -16);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-16\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm, 15);
                  remainder = imm - (times * 15);

                  for (int i = 0; i < times; i++) {
                    len = snprintf(NULL, 0, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);
                    if (len < 0) {
                      perror("snprintf");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                    
                    instr = (char *)malloc((size_t)len + 1);
                    if (!instr) {
                      perror("malloc");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    snprintf(instr, (size_t)len + 1, "ADD %s, %s, #15\n", lhs_reg, lhs_reg);

                    if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                      perror("write");
                                      
                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(instr);
                      instr = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD %s, %s, #%d\n", lhs_reg, lhs_reg, remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
            }
          }
          else {
            // tREG -= <term>
            
            if (strstr(rhs->tag, "tREG|regex") != NULL) {
              // tREG -= tREG
              
              rhs_reg = strdup(rhs->contents);
              if (!rhs_reg) {
                perror("strdup");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (strcmp(lhs_reg, rhs_reg) == 0) {
                len = snprintf(NULL, 0, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "AND %s, %s, #0\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
              }
              else {
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_reg);
                  rhs_reg = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tREG -= tID
              
              rhs_id = strdup(rhs->contents);
              if (!rhs_id) {
                perror("strdup");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "NOT R5, R5\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              const_instr = "ADD R5, R5, #1\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_id);
              rhs_id = NULL;
              free(instr);
              instr = NULL;
              
              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          return EXIT_FAILURE;
              }
            }
            else {
              // tREG -= tINT
              
              rhs_int = strdup(rhs->contents);
              if (!rhs_int) {
                perror("strdup");
                free(lhs_reg);
                lhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                free(lhs_reg);
                lhs_reg = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm != 0) {
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                                      
                  
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                if ((imm >= -16) && (imm <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm <= -16) {
                  times = floor_div(imm, -16);
                  remainder = imm - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm, 15);
                  remainder = imm - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_reg);
                      lhs_reg = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      
                      return EXIT_FAILURE;
                    }

                    free(instr);
                    instr = NULL;
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_reg);
                    lhs_reg = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;

                }

                const_instr = "NOT R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                len = snprintf(NULL, 0, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "ADD %s, %s, R5\n", lhs_reg, lhs_reg);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_reg);
                  lhs_reg = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              free(lhs_reg);
              lhs_reg = NULL;
              free(rhs_int);
              rhs_int = NULL;
            }
          }
        }
        else {
          lhs_id = strdup(lhs->contents);
          if (!lhs_id) {
            perror("strdup");
                  
            return EXIT_FAILURE;
          }

          if (!find_var(*var_list, lhs_id, &buf, 0)) {
            fprintf(stderr, "ERROR: Undefined variable: %s\n", lhs_id);
                      
            free(lhs_id);
            lhs_id = NULL;
            
            return EXIT_FAILURE;
          }

          if (strstr(op->tag, "tPLUSASSIGN|regex") != NULL) {
            // tID += <term>
            
            if (strstr(rhs->tag, "tREG|regex") != NULL) {
              // tID += tREG
              
              rhs_reg = strdup(rhs->contents);
              if (!rhs_reg) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
              
              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          return EXIT_FAILURE;
              }
            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tID += tID
              
              rhs_id = strdup(rhs->contents);
              if (!rhs_id) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (strcmp(lhs_id, rhs_id) == 0) {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                const_instr = "ADD R5, R5, R5\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
              else {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;
                
                status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                const_instr = "ADD R5, R5, R6\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }

                const_instr = "AND R6, R6, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
            }
            else {
              // tID += tINT
              rhs_int = strdup(rhs->contents);
              if (!rhs_int) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm != 0) {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if ((imm >= -16) && (imm <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", imm);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", imm);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm <= -16) {
                  times = floor_div(imm, -16);
                  remainder = imm - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(var_addr);
                      var_addr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm, 15);
                  remainder = imm - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(var_addr);
                      var_addr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
            }
          }
          else {
            // tID -= <term>
            
            if (strstr(rhs->tag, "tREG|regex") != NULL) {
              // tID -= tREG
              
              rhs_reg = strdup(rhs->contents);
              if (!rhs_reg) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

              if (!status) {
                fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_reg, rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
              
              len = snprintf(NULL, 0, "ADD %s, %s, #1\n", rhs_reg, rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #1\n", rhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "ADD R5, R5, %s\n", rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD R5, R5, %s\n", rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;
              
              len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              const_instr = "AND R5, R5, #0\n";

              if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              len = snprintf(NULL, 0, "ADD %s, %s, #-1\n", rhs_reg, rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "ADD %s, %s, #-1\n", rhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(instr);
              instr = NULL;

              len = snprintf(NULL, 0, "NOT %s, %s\n", rhs_reg, rhs_reg);

              if (len < 0) {
                perror("snprintf");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }
              
              instr = (char *)malloc((size_t)len + 1);
              if (!instr) {
                perror("malloc");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                
                return EXIT_FAILURE;
              }

              snprintf(instr, (size_t)len + 1, "NOT %s, %s\n", rhs_reg, rhs_reg);

              if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                perror("write");
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_reg);
                rhs_reg = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                return EXIT_FAILURE;
              }

              free(lhs_id);
              lhs_id = NULL;
              free(rhs_reg);
              rhs_reg = NULL;
              free(var_addr);
              var_addr = NULL;
              free(instr);
              instr = NULL;
            }
            else if (strstr(rhs->tag, "tID|regex") != NULL) {
              // tID -= tID
              
              rhs_id = strdup(rhs->contents);
              if (!rhs_id) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (!find_var(*var_list, rhs_id, &buf, 0)) {
                fprintf(stderr, "ERROR: Undefined variable: %s\n", rhs_id);
                          
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (strcmp(lhs_id, rhs_id) == 0) {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
              }
              else {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                status = get_var_address(var_list, rhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R6, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R6, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "NOT R6, R6\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R6, R6, #1\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                const_instr = "ADD R5, R5, R6\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_id);
                  rhs_id = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }
              
                free(lhs_id);
                lhs_id = NULL;
                free(rhs_id);
                rhs_id = NULL;
                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;

                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }

                const_instr = "AND R6, R6, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  return EXIT_FAILURE;
                }
              }
            }
            else {
              // tID -= tINT
              
              rhs_int = strdup(rhs->contents);
              if (!rhs_int) {
                perror("strdup");
                          free(lhs_id);
                lhs_id = NULL;
                
                return EXIT_FAILURE;
              }

              if (sscanf(rhs_int, "%d", &imm) != 1) {
                fprintf(stderr, "ERROR: Failed to parse integer value: '%s'\n", rhs_int);
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }
              else if ((imm <= -4080) || (imm >= 3825)) {
                fprintf(stderr, "ERROR: Integer outside the allowed range ([-4080, 3825]): %d\n", imm);
                          free(lhs_id);
                lhs_id = NULL;
                free(rhs_int);
                rhs_int = NULL;
                
                return EXIT_FAILURE;
              }

              if (imm != 0) {
                status = get_var_address(var_list, lhs_id, next_addr, &var_addr);

                if (!status) {
                  fprintf(stderr, "ERROR: Variable address could not be retrieved\n");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }

                len = snprintf(NULL, 0, "LDR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "LDR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(instr);
                instr = NULL;

                if ((imm >= -16) && (imm <= 15)) {
                  len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", imm);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", imm);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else if (imm <= -16) {
                  times = floor_div(imm, -16);
                  remainder = imm - (times * -16);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #16\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(var_addr);
                      var_addr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }
                else {
                  times = floor_div(imm, 15);
                  remainder = imm - (times * 15);

                  for (int i = 0; i < times; i++) {
                    const_instr = "ADD R5, R5, #-15\n";

                    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                      perror("write");
                                      free(lhs_id);
                      lhs_id = NULL;
                      free(rhs_int);
                      rhs_int = NULL;
                      free(var_addr);
                      var_addr = NULL;
                      
                      return EXIT_FAILURE;
                    }
                  }

                  len = snprintf(NULL, 0, "ADD R5, R5, #-%d\n", remainder);
                  if (len < 0) {
                    perror("snprintf");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }
                  
                  instr = (char *)malloc((size_t)len + 1);
                  if (!instr) {
                    perror("malloc");
                                  
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  snprintf(instr, (size_t)len + 1, "ADD R5, R5, #-%d\n", remainder);

                  if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                    perror("write");
                    
                    free(lhs_id);
                    lhs_id = NULL;
                    free(rhs_int);
                    rhs_int = NULL;
                    free(var_addr);
                    var_addr = NULL;
                    free(instr);
                    instr = NULL;
                    
                    return EXIT_FAILURE;
                  }

                  free(instr);
                  instr = NULL;
                }

                len = snprintf(NULL, 0, "STR R5, R7, %s\n", var_addr);

                if (len < 0) {
                  perror("snprintf");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }
                
                instr = (char *)malloc((size_t)len + 1);
                if (!instr) {
                  perror("malloc");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  
                  return EXIT_FAILURE;
                }

                snprintf(instr, (size_t)len + 1, "STR R5, R7, %s\n", var_addr);

                if (write_instruction(write_fd, instr, (size_t)len) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  free(var_addr);
                  var_addr = NULL;
                  free(instr);
                  instr = NULL;
                  
                  return EXIT_FAILURE;
                }

                free(var_addr);
                var_addr = NULL;
                free(instr);
                instr = NULL;
                
                const_instr = "AND R5, R5, #0\n";

                if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
                  perror("write");
                              
                  free(lhs_id);
                  lhs_id = NULL;
                  free(rhs_int);
                  rhs_int = NULL;
                  
                  return EXIT_FAILURE;
                }
              }

              free(lhs_id);
              lhs_id = NULL;
              free(rhs_int);
              rhs_int = NULL;
            }
          }
        }
      }
      
      else if (strstr(ast_next->tag, "brk_stmt|>") != NULL) {
        mpc_ast_t *page = ast_next->children[2];
        mpc_ast_t *write = ast_next->children[4];
        mpc_ast_t *read = ast_next->children[6];
        mpc_ast_t *alloc = ast_next->children[8];

        int vpn = 0;
        if (sscanf(page->contents, "%d", &vpn) != 1) {
          fprintf(stderr, "ERROR: Failed to parse page number: '%s'\n", page->contents);
              
          return EXIT_FAILURE;
        }

        int write_mode = 0;
        if (sscanf(write->contents, "%d", &write_mode) != 1) {
          fprintf(stderr, "ERROR: Failed to parse write mode: '%s'\n", write->contents);
              
          return EXIT_FAILURE;
        }

        int read_mode = 0;
        if (sscanf(read->contents, "%d", &read_mode) != 1) {
          fprintf(stderr, "ERROR: Failed to parse read mode: '%s'\n", read->contents);
              
          return EXIT_FAILURE;
        }

        int alloc_mode = 0;
        if (sscanf(alloc->contents, "%d", &alloc_mode) != 1) {
          fprintf(stderr, "ERROR: Failed to parse allocation/deallocation mode: '%s'\n", alloc->contents);
              
          return EXIT_FAILURE;
        }

        if ((vpn < 0) || (vpn >= 32) || (write_mode < 0) || (write_mode > 1) || (read_mode < 0) || (read_mode > 1) || (alloc_mode < 0) || (alloc_mode > 1)) {
          fprintf(stderr, "ERROR: Incorrect BRK expression: BRK(%d,%d,%d,%d)\n", vpn, write_mode, read_mode, alloc_mode);
              
          return EXIT_FAILURE;
        }

        int16_t brk_value = (vpn << 11) | (write_mode << 2) | (read_mode << 1) | alloc_mode;
        const_instr = "AND R0, R0, #0\n";

        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
          perror("write");
              
          return EXIT_FAILURE;
        }

        if ((brk_value >= -16) && (brk_value <= 15)) {
          len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", brk_value);
          if (len < 0) {
            perror("snprintf");
                  
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                  
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", brk_value);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                  
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
        }
        else if (brk_value <= -16) {
          times = floor_div(brk_value, -16);
          remainder = brk_value - (times * -16);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R0, R0, #-16\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
                      
              return EXIT_FAILURE;
            }
          }

          len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
                  
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
                  
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
                  
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
        }
        else {
          times = floor_div(brk_value, 15);
          remainder = brk_value - (times * 15);

          for (int i = 0; i < times; i++) {
            const_instr = "ADD R0, R0, #15\n";

            if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
              perror("write");
              
              return EXIT_FAILURE;
            }
          }

          len = snprintf(NULL, 0, "ADD R0, R0, #%d\n", remainder);
          if (len < 0) {
            perror("snprintf");
            
            return EXIT_FAILURE;
          }
          
          instr = (char *)malloc((size_t)len + 1);
          if (!instr) {
            perror("malloc");
            
            return EXIT_FAILURE;
          }

          snprintf(instr, (size_t)len + 1, "ADD R0, R0, #%d\n", remainder);

          if (write_instruction(write_fd, instr, (size_t)len) == -1) {
            perror("write");
            
            free(instr);
            instr = NULL;
            
            return EXIT_FAILURE;
          }

          free(instr);
          instr = NULL;
        }

        const_instr = "BRK\n";
        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
          perror("write");
          
          return EXIT_FAILURE;
        }
      }
      
      else if (strstr(ast_next->tag, "yield_stmt|>") != NULL) {
        const_instr = "YIELD\n";
        if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
          perror("write");
          
          return EXIT_FAILURE;
        }

        const_instr = NULL;
      }
      else if (strstr(ast_next->tag, "unmatched_if|>") != NULL) {
        // Get the Boolean expression
        mpc_ast_t *bool_node = ast_next->children[2];
        // Get the body of the if clause
        mpc_ast_t *then_node = ast_next->children[5];
        
        // Generate the labels
        char *cond_label = NULL;
        char *end_label = NULL;

        if ((!get_unique_label("L", label_counter, &cond_label)) || (!get_unique_label("L", label_counter, &end_label))) {
          fprintf(stderr, "ERROR: Labels could not be generated\n");
          
          return EXIT_FAILURE;
        }

        // Emit the condition label
        len = snprintf(NULL, 0, "%s\n", cond_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", cond_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the Boolean expression
        if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
          fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
          
          return EXIT_FAILURE;
        }

        // If the condition is false, branch to the end label
        len = snprintf(NULL, 0, "BRz %s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "BRz %s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the body of the if clause
        status = compile(then_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);
        if (status == EXIT_FAILURE) {
          fprintf(stderr, "ERROR: Compilation error inside then-body of unmatched_if\n");
          
          return EXIT_FAILURE;
        }

        // Emit the end label
        len = snprintf(NULL, 0, "%s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }
      else if (strstr(ast_next->tag, "matched_if|>") != NULL) {
        // Get the Boolean expression
        mpc_ast_t *bool_node = ast_next->children[2];
        // Get the body of the if clause
        mpc_ast_t *then_node = ast_next->children[5];
        // Get the body of the else clause
        mpc_ast_t *else_node = ast_next->children[8];

        // Generate the labels
        char *cond_label = NULL;
        char *else_label = NULL;
        char *end_label = NULL;

        if ((!get_unique_label("L", label_counter, &cond_label)) || (!get_unique_label("L", label_counter, &else_label)) || (!get_unique_label("L", label_counter, &end_label))) {
          fprintf(stderr, "ERROR: Labels could not be generated\n");
          
          return EXIT_FAILURE;
        }

        // Emit the condition label
        len = snprintf(NULL, 0, "%s\n", cond_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", cond_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the Boolean expression
        if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
          fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
          
          return EXIT_FAILURE;
        }

        // If the condition is false, branch to the else label
        len = snprintf(NULL, 0, "BRz %s\n", else_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "BRz %s\n", else_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the body of the if clause
        status = compile(then_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);

        if (status == EXIT_FAILURE) {
          fprintf(stderr, "ERROR: Compilation error inside then-body of matched_if\n");
          
          return EXIT_FAILURE;
        }

        // Finish the if clause by jumping to the end label
        len = snprintf(NULL, 0, "BR %s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "BR %s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Emit the else label
        len = snprintf(NULL, 0, "%s\n", else_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", else_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the body of the else clause
        status = compile(else_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);
        
        if (status == EXIT_FAILURE) {
          fprintf(stderr, "ERROR: Compilation error inside else-body of matched_if\n");
          
          return EXIT_FAILURE;
        }

        // Emit the end label
        len = snprintf(NULL, 0, "%s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }
      else if (strstr(ast_next->tag, "while_stmt|>") != NULL) {
        // Get the Boolean expression
        mpc_ast_t *bool_node = ast_next->children[2];
        // Get the body
        mpc_ast_t *body_node = ast_next->children[5];
        
        // Generate the labels
        char *start_label = NULL;
        char *end_label = NULL;

        if ((!get_unique_label("L", label_counter, &start_label)) || (!get_unique_label("L", label_counter, &end_label))) {
          fprintf(stderr, "ERROR: Labels could not be generated\n");
          
          return EXIT_FAILURE;
        }

        // Emit the start label
        len = snprintf(NULL, 0, "%s\n", start_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", start_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Compile the Boolean expression
        if (!compile_boolean(bool_node, *var_list, label_counter, write_fd)) {
          fprintf(stderr, "ERROR: Boolean expression could not be compiled\n");
          
          return EXIT_FAILURE;
        }

        // If the condition is false, branch to the end label
        len = snprintf(NULL, 0, "BRz %s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "BRz %s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // Recursive call for the body of the loop
        status = compile(body_node, var_list, heap_list, next_addr, 0, label_counter, write_fd);

        if (status == EXIT_FAILURE) {
          fprintf(stderr, "ERROR: Compilation error inside while loop\n");
          
          return EXIT_FAILURE;
        }

        // The loop body is compiled

        // Jump back to the start label
        len = snprintf(NULL, 0, "BR %s\n", start_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "BR %s\n", start_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;

        // End label
        len = snprintf(NULL, 0, "%s\n", end_label);
        if (len < 0) {
          perror("snprintf");
          
          return EXIT_FAILURE;
        }

        instr = (char *)malloc((size_t)len + 1);
        if (!instr) {
          perror("malloc");
          
          return EXIT_FAILURE;
        }

        snprintf(instr, (size_t)len + 1, "%s\n", end_label);
        
        if (write_instruction(write_fd, instr, (size_t)len) == -1) {
          perror("write");
          
          free(instr);
          instr = NULL;
          
          return EXIT_FAILURE;
        }

        free(instr);
        instr = NULL;
      }
    }
  }

  if (init) {
    const_instr = "YIELD\n";
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      
      return EXIT_FAILURE;
    }

    const_instr = "HALT\n";
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      
      return EXIT_FAILURE;
    }

    const_instr = ".END\n";
    if (write_instruction(write_fd, const_instr, strlen(const_instr)) == -1) {
      perror("write");
      
      return EXIT_FAILURE;
    }
  }

  return EXIT_SUCCESS;
}

int generate_heap(const char *filename, lc3c_heap_list *heap_list) {
  if (!filename) {
    fprintf(stderr, "ERROR: NULL filename passed to generate_heap\n");
    return EXIT_FAILURE;
  }

  const char *dot = strrchr(filename, '.');
  size_t base_len;
  if (dot) {
    base_len = (size_t)(dot - filename);
  } else {
    base_len = strlen(filename);
  }

  // Suffix "_heap.obj"
  char *outname = (char *)malloc(base_len + 10);
  if (!outname) {
    perror("malloc");
    return EXIT_FAILURE;
  }

  memcpy(outname, filename, base_len);
  memcpy(outname + base_len, "_heap.obj", 10);

  int fd = open(outname, O_WRONLY | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
  if (fd == -1) {
    perror("open");
    free(outname);
    outname = NULL;
    
    return EXIT_FAILURE;
  }

  lc3c_heap_list *current = heap_list;

  while (current) {
    int value = current->heap_rec->value;

    // 16-bit signed range is checked
    if (value < -65536 || value > 65535) {
      fprintf(stderr, "ERROR: Value %d outside the range for 16-bit numbers\n", value);
      close(fd);
      free(outname);
      outname = NULL;
      
      return EXIT_FAILURE;
    }

    // Store as unsigned 16-bit, little-endian
    uint16_t uval = (uint16_t)value;
    uint8_t bytes[2];
    bytes[0] = (uint8_t)(uval & 0xFF);
    bytes[1] = (uint8_t)((uval >> 8) & 0xFF);

    if (write_instruction(fd, bytes, sizeof(bytes)) == -1) {
      perror("write");
      close(fd);
      free(outname);
      outname = NULL;

      return EXIT_FAILURE;
    }

    current = current->next;
  }

  close(fd);
  free(outname);
  outname = NULL;
  
  return EXIT_SUCCESS;
}

int compile_file(const char *in_filename, const char *out_filename) {
  // The variables to be used during the compilation
  lc3c_var_address_list *vars = NULL;
  lc3c_heap_list *init_heap = NULL;
  int unique_label_counter = 0;
  int next_var_address = 0x0000;

  // The language specification:
  // Non-terminals
  mpc_parser_t *lc3 = mpc_new("lc3");
  mpc_parser_t *stmts = mpc_new("stmts");
  mpc_parser_t *stmt = mpc_new("stmt");
  mpc_parser_t *assignment = mpc_new("assignment");
  mpc_parser_t *arithmetic = mpc_new("arithmetic");
  mpc_parser_t *term = mpc_new("term");
  mpc_parser_t *selfarithmetic = mpc_new("selfarithmetic");
  mpc_parser_t *matched_if = mpc_new("matched_if");
  mpc_parser_t *matched_stmts = mpc_new("matched_stmts");
  mpc_parser_t *matched_stmt = mpc_new("matched_stmt");
  mpc_parser_t *unmatched_if = mpc_new("unmatched_if");
  mpc_parser_t *while_stmt = mpc_new("while_stmt");
  mpc_parser_t *unmatched_stmts = mpc_new("unmatched_stmts");
  mpc_parser_t *unmatched_stmt = mpc_new("unmatched_stmt");
  mpc_parser_t *boolean = mpc_new("boolean");
  mpc_parser_t *brk_stmt = mpc_new("brk_stmt");
  mpc_parser_t *yield_stmt = mpc_new("yield_stmt");
  
  // Tokens
  mpc_parser_t *tREG = mpc_new("tREG");
  mpc_parser_t *tID = mpc_new("tID");
  mpc_parser_t *tINT = mpc_new("tINT");
  mpc_parser_t *tASSIGN = mpc_new("tASSIGN");
  mpc_parser_t *tPLUSASSIGN = mpc_new("tPLUSASSIGN");
  mpc_parser_t *tMINUSASSIGN = mpc_new("tMINUSASSIGN");
  mpc_parser_t *tPLUS = mpc_new("tPLUS");
  mpc_parser_t *tMINUS = mpc_new("tMINUS");
  mpc_parser_t *tEQ = mpc_new("tEQ");
  mpc_parser_t *tNEQ = mpc_new("tNEQ");
  mpc_parser_t *tLT = mpc_new("tLT");
  mpc_parser_t *tGT = mpc_new("tGT");
  mpc_parser_t *tLTE = mpc_new("tLTE");
  mpc_parser_t *tGTE = mpc_new("tGTE");
  mpc_parser_t *tLPR = mpc_new("tLPR");
  mpc_parser_t *tRPR = mpc_new("tRPR");
  mpc_parser_t *tCOMMA = mpc_new("tCOMMA");
  mpc_parser_t *tIF = mpc_new("tIF");
  mpc_parser_t *tELSE = mpc_new("tELSE");
  mpc_parser_t *tWHILE = mpc_new("tWHILE");
  mpc_parser_t *tEND = mpc_new("tEND");
  mpc_parser_t *tBRK = mpc_new("tBRK");
  mpc_parser_t *tYIELD = mpc_new("tYIELD");
  mpc_parser_t *tCOLON = mpc_new("tCOLON");
  mpc_parser_t *tSEMICOLON = mpc_new("tSEMICOLON");

  // CFG for the language:
  mpc_err_t *err = mpca_lang(
    MPCA_LANG_DEFAULT,
    "lc3                : /^/ <stmts> /$/ ;                                                                                   \n"
    "stmts              : <stmt>* ;                                                                                           \n"
    "stmt               : <assignment>                                                                                        \n"
    "                   | <selfarithmetic>                                                                                    \n"
    "                   | <matched_if>                                                                                        \n"
    "                   | <unmatched_if>                                                                                      \n"
    "                   | <while_stmt>                                                                                        \n"
    "                   | <brk_stmt>                                                                                          \n"
    "                   | <yield_stmt> ;                                                                                      \n"
    "assignment         : <tREG> <tASSIGN> <arithmetic> <tSEMICOLON>                                                          \n"
    "                   | <tID> <tASSIGN> <arithmetic> <tSEMICOLON> ;                                                         \n"
    "arithmetic         : <term> (<tPLUS> | <tMINUS>) <term>                                                                  \n"
    "                   | <term> ;                                                                                            \n"
    "term               : <tREG>                                                                                              \n"
    "                   | <tID>                                                                                               \n"
    "                   | <tINT> ;                                                                                            \n"
    "selfarithmetic     : <tREG> <tPLUSASSIGN> <term> <tSEMICOLON>                                                            \n"
    "                   | <tREG> <tMINUSASSIGN> <term> <tSEMICOLON>                                                           \n"
    "                   | <tID> <tPLUSASSIGN> <term> <tSEMICOLON>                                                             \n"
    "                   | <tID> <tMINUSASSIGN> <term> <tSEMICOLON> ;                                                          \n"
    "matched_if         : <tIF> <tLPR> <boolean> <tRPR> <tCOLON> <matched_stmts> <tELSE> <tCOLON> <unmatched_stmts> <tEND> ;  \n"
    "matched_stmts      : <matched_stmt>* ;                                                                                   \n"
    "matched_stmt       : <assignment> | <selfarithmetic> | <matched_if> | <while_stmt> | <yield_stmt> ;                      \n"
    "unmatched_if       : <tIF> <tLPR> <boolean> <tRPR> <tCOLON> <unmatched_stmts> <tEND> ;                                   \n"
    "while_stmt         : <tWHILE> <tLPR> <boolean> <tRPR> <tCOLON> <unmatched_stmts> <tEND> ;                                \n"
    "unmatched_stmts    : <unmatched_stmt>* ;                                                                                 \n"
    "unmatched_stmt     : <assignment> | <selfarithmetic> | <unmatched_if> | <matched_if> | <while_stmt> | <yield_stmt> ;     \n"
    "boolean            : <term> <tLTE> <term>                                                                                \n"
    "                   | <term> <tGTE> <term>                                                                                \n"
    "                   | <term> <tNEQ> <term>                                                                                \n"
    "                   | <term> <tEQ> <term>                                                                                 \n"
    "                   | <term> <tLT> <term>                                                                                 \n"
    "                   | <term> <tGT> <term> ;                                                                               \n"
    "brk_stmt           : <tBRK> <tLPR> <tINT> <tCOMMA> <tINT> <tCOMMA> <tINT> <tCOMMA> <tINT> <tRPR> <tSEMICOLON> ;          \n"
    "yield_stmt         : <tYIELD> <tSEMICOLON> ;                                                                             \n"
    "tIF                : \"if\" ;                                                                                            \n"
    "tELSE              : \"else\" ;                                                                                          \n"
    "tWHILE             : \"while\" ;                                                                                         \n"
    "tEND               : \"end\" ;                                                                                           \n"
    "tBRK               : \"BRK\" ;                                                                                           \n"
    "tYIELD             : \"YIELD\" ;                                                                                         \n"
    "tREG               : /R[1-4]/ ;                                                                                          \n"
    "tID                : /[a-zA-Z][a-zA-Z0-9]*/ ;                                                                            \n"
    "tINT               : /-?[1-9][0-9]*|0/ ;                                                                                 \n"
    "tASSIGN            : /=/ ;                                                                                               \n"
    "tPLUSASSIGN        : /\\+=/ ;                                                                                            \n"
    "tMINUSASSIGN       : /-=/ ;                                                                                              \n"
    "tPLUS              : /\\+/ ;                                                                                             \n"
    "tMINUS             : /-/ ;                                                                                               \n"
    "tEQ                : /==/ ;                                                                                              \n"
    "tNEQ               : /!=/ ;                                                                                              \n"
    "tLT                : /</ ;                                                                                               \n"
    "tGT                : />/ ;                                                                                               \n"
    "tLTE               : /<=/ ;                                                                                              \n"
    "tGTE               : />=/ ;                                                                                              \n"
    "tLPR               : /\\(/ ;                                                                                             \n"
    "tRPR               : /\\)/ ;                                                                                             \n"
    "tCOMMA             : /,/ ;                                                                                               \n"
    "tCOLON             : /:/ ;                                                                                               \n"
    "tSEMICOLON         : /;/ ;                                                                                               \n",
    lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic, matched_if,
    matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt, boolean, brk_stmt,
    yield_stmt, tIF, tELSE, tWHILE, tEND, tBRK, tYIELD, tREG, tID, tINT, tASSIGN,
    tPLUSASSIGN, tMINUSASSIGN, tPLUS, tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR,
    tRPR, tCOMMA, tCOLON, tSEMICOLON, NULL
  );

  if (err != NULL) {
    mpc_err_print(err);
    mpc_err_delete(err);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);

    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    return EXIT_FAILURE;
  }

  int read_fd = open(in_filename, O_RDONLY);
  if (read_fd == -1) {
    perror("open"); // Print the system error message for open()

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);
    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    return EXIT_FAILURE;
  }

  struct stat st;
  if (fstat(read_fd, &st) == -1) {
    perror("fstat"); // Print the system error message for fstat()
    close(read_fd);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);
    
    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    return EXIT_FAILURE;
  }

  size_t filesize = (size_t)st.st_size;
  
  char *source_code = (char *)malloc(filesize + 1);
  if (!source_code) {
    perror("malloc"); // Print the system error message for malloc()
    close(read_fd);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);
      
    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    return EXIT_FAILURE;
  }
  
  ssize_t total_read = 0;
  while ((size_t)total_read < filesize) {
    ssize_t read_bytes = read(read_fd, source_code + total_read, filesize - (size_t)total_read);

    if (read_bytes == -1) {
      if (errno == EINTR) { // Interrupted system call
        continue;
      }
      perror("read"); // Print the system error message for read()

      free(source_code);
      close(read_fd);

      mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
        matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
        boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
        tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
        tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);

      var_address_list_free(vars);
      heap_list_free(init_heap);
      
      return EXIT_FAILURE;
    }

    if (read_bytes == 0) {
      break;
    }

    total_read += read_bytes;
  }
  source_code[total_read] = '\0';
  close(read_fd);

  char *clean_source_code = strip_comments(source_code);
  if (!clean_source_code) {
    free(source_code);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);

    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    exit(EXIT_FAILURE);
  }
  
  free(source_code);

  mpc_result_t result;

  if (!mpc_parse(in_filename, clean_source_code, lc3, &result)) {
    if (result.error) {
      mpc_err_print(result.error);
      mpc_err_delete(result.error);
    }

    free(clean_source_code);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);

    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    exit(EXIT_FAILURE);
  }

  int status = EXIT_FAILURE;

  int write_fd = open(out_filename, O_WRONLY | O_CREAT | O_TRUNC, S_IRUSR | S_IWUSR | S_IRGRP | S_IROTH);
  if (write_fd == -1) {
    perror("open");

    free(clean_source_code);

    mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
      matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
      boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
      tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
      tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);

    var_address_list_free(vars);
    heap_list_free(init_heap);
    
    return EXIT_FAILURE;
  }

  if (result.output) {
    mpc_ast_t *ast = (mpc_ast_t *)(result.output);
    
    status = compile(ast, &vars, &init_heap, &next_var_address, 1, &unique_label_counter, write_fd);
    
    close(write_fd);
    mpc_ast_delete(ast);

    if (status == EXIT_SUCCESS) {
      printf("%s is successfully compiled\n", in_filename);

      status = generate_heap(in_filename, init_heap);

      if (status == EXIT_FAILURE) {
        fprintf(stderr, "ERROR: Heap could not be generated for %s\n", in_filename);
      }
      else {
        printf("Heap is successfully generated for %s\n", in_filename);
      }
    }
    else {
      fprintf(stderr, "ERROR: %s could not be compiled\n", in_filename);
    }
  }
  else {
    close(write_fd);
  }

  free(clean_source_code);

  mpc_cleanup(42, lc3, stmts, stmt, assignment, arithmetic, term, selfarithmetic,
    matched_if, matched_stmts, matched_stmt, unmatched_if, while_stmt, unmatched_stmts, unmatched_stmt,
    boolean, brk_stmt, yield_stmt, tREG, tID, tINT, tASSIGN, tPLUSASSIGN, tMINUSASSIGN, tPLUS,
    tMINUS, tEQ, tNEQ, tLT, tGT, tLTE, tGTE, tLPR, tRPR, tCOMMA, tIF, tELSE, tWHILE,
    tEND, tBRK, tYIELD, tCOLON, tSEMICOLON);
  
  var_address_list_free(vars);
  heap_list_free(init_heap);
  
  return status;
}

// Assembler code
// The maximum number of labels = Number of possible memory locations in a 16-bit address space
#define MAX_LABELS 65536
// A redundantly large number of tokens are allowed exist in a line
#define MAX_TOKENS 10
// A redundantly large number of characters are allowed exist in a line
#define MAX_LINE_LENGTH 1024

typedef struct {
  char *name;
  int address;
} lc3a_label;

typedef struct lc3a_label_list {
  lc3a_label *label_data;
  struct lc3a_label_list *next;
} lc3a_label_list;

typedef struct lc3a_line_list {
  char *line_data;
  struct lc3a_line_list *next;
} lc3a_line_list;

typedef struct lc3a_mach_code_list {
  uint16_t *mach_code;
  struct lc3a_mach_code_list *next;
} lc3a_mach_code_list;

void label_list_free(lc3a_label_list *label_list);
void line_list_free(lc3a_line_list *line_list);
void machine_code_free(lc3a_mach_code_list *mach_code_list);

void label_list_count(lc3a_label_list *label_list, int *result) {
  int count = 0;
  lc3a_label_list *current = label_list;

  while (current) {
    count++;
    current = current->next;
  }

  *result = count;
}

int label_list_add(lc3a_label_list **label_list, const char *name, int address) {
  int label_count = 0;
  label_list_count(*label_list, &label_count);
  if (label_count == MAX_LABELS) {
    fprintf(stderr, "ERROR: Maximum label number (%d) reached\n", MAX_LABELS);
    return 0;
  }

  lc3a_label_list *current = *label_list;
  lc3a_label_list *prev = current;
  int len = 0;

  while (current) {
    prev = current;
    current = current->next;
  }

  current = (lc3a_label_list *)malloc(sizeof(lc3a_label_list));
  if (!current) {
    perror("malloc");
    return 0;
  }

  current->label_data = (lc3a_label *)malloc(sizeof(lc3a_label));
  if (!(current->label_data)) {
    perror("malloc");
    free(current);
    current = NULL;
    return 0;
  }

  len = snprintf(NULL, 0, "%s", name);
  if (len < 0) {
    perror("snprintf");
    free(current->label_data);
    current->label_data = NULL;
    free(current);
    current = NULL;
    return 0;
  }

  current->label_data->name = (char *)malloc((size_t)len + 1);
  if (!(current->label_data->name)) {
    perror("malloc");
    free(current->label_data);
    current->label_data = NULL;
    free(current);
    current = NULL;
    return 0;
  }

  strcpy(current->label_data->name, name);

  current->label_data->address = address;
  current->next = NULL;

  if (prev) {
    prev->next = current;
  }
  else {
    *label_list = current;
  }

  return 1;
}

void label_list_free(lc3a_label_list *label_list) {
  lc3a_label_list *current = label_list;
  lc3a_label_list *temp = NULL;

  while (current) {
    temp = current;
    current = current->next;

    free(temp->label_data->name);
    temp->label_data->name = NULL;
    free(temp->label_data);
    temp->label_data = NULL;
    free(temp);
    temp = NULL;
  }
}

int line_list_add(lc3a_line_list **line_list, const char *line) {
  lc3a_line_list *current = *line_list;
  lc3a_line_list *prev = current;
  int len = 0;

  while (current) {
    prev = current;
    current = current->next;
  }

  current = (lc3a_line_list *)malloc(sizeof(lc3a_line_list));
  if (!current) {
    perror("malloc");
    return 0;
  }

  len = snprintf(NULL, 0, "%s", line);
  if (len < 0) {
    perror("snprintf");
    free(current);
    current = NULL;
    return 0;
  }

  current->line_data = (char *)malloc((size_t)len + 1);
  if (!(current->line_data)) {
    perror("malloc");
    free(current);
    current = NULL;
    return 0;
  }

  strcpy(current->line_data, line);
  current->next = NULL;

  if (prev) {
    prev->next = current;
  }
  else {
    *line_list = current;
  }

  return 1;
}

void line_list_free(lc3a_line_list *line_list) {
  lc3a_line_list *current = line_list;
  lc3a_line_list *temp = NULL;

  while (current) {
    temp = current;
    current = current->next;

    free(temp->line_data);
    temp->line_data = NULL;
    free(temp);
    temp = NULL;
  }
}

int machine_code_add(lc3a_mach_code_list **mach_code_list, uint16_t word) {
  lc3a_mach_code_list *current = *mach_code_list;
  lc3a_mach_code_list *prev = current;

  while (current) {
    prev = current;
    current = current->next;
  }

  current = (lc3a_mach_code_list *)malloc(sizeof(lc3a_mach_code_list));
  if (!current) {
    perror("malloc");
    return 0;
  }
  current->mach_code = (uint16_t *)malloc(sizeof(uint16_t));
  if (!(current->mach_code)) {
    perror("malloc");
    free(current);
    current = NULL;
    return 0;
  }
  *(current->mach_code) = word;
  current->next = NULL;

  if (prev) {
    prev->next = current;
  }
  else {
    *mach_code_list = current;
  }

  return 1;
}

void machine_code_free(lc3a_mach_code_list *mach_code_list) {
  lc3a_mach_code_list *current = mach_code_list;
  lc3a_mach_code_list *temp = NULL;

  while (current) {
    temp = current;
    current = current->next;

    free(temp->mach_code);
    temp->mach_code = NULL;
    free(temp);
    temp = NULL;
  }
}

// Helper function to trim whitespace
void trim(char *str) {
  char *start = str;
  char *end;
  
  // Trim leading space
  while(isspace((unsigned char)*start)) {
    start++;
  }
  
  if (*start == 0) {
    str[0] = '\0';
    return;
  }
  
  // Trim trailing space
  end = start + strlen(start) - 1;
  while (end > start && isspace((unsigned char)*end)) {
    end--;
  }
  end[1] = '\0';
  
  // Move trimmed string to beginning
  if (start != str) {
    memmove(str, start, strlen(start) + 1);
  }
}

// Parses the number from a numeric token such as #12 and x3000
int parse_number(const char *s, int *result) {
  if (s[0] == '#') {
    *result = atoi(s + 1);
    return 1;
  } else if (s[0] == 'x') {
    *result = (int)strtol(s + 1, NULL, 16);
    return 1;
  }
  return 0;
}

// Finds the address of a label from the label list given its name
int lookup_label(lc3a_label_list *label_list, const char *name, int *result) {
  lc3a_label_list *current_label = label_list;

  while (current_label) {
    if (strcmp(current_label->label_data->name, name) == 0) {
      *result = current_label->label_data->address;
      return 1;
    }

    current_label = current_label->next;
  }

  fprintf(stderr, "ERROR: Unknown label <%s>\n", name);
  return 0;
}

void free_tokens(char **tokens, int count) {
  for (int i = 0; i < count; i++) {
    free(tokens[i]);
    tokens[i] = NULL;
  }
  free(tokens);
  tokens = NULL;
}

// A helper function to tokenize a given line
int tokenize(char *line, int *token_count, char ***result) {
  char **tokens = NULL;
  int count = 0;
  int capacity = MAX_TOKENS;

  tokens = (char **)malloc(capacity * sizeof(char *));
  if (!tokens) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory allocation failed for tokens\n");
    return 0;
  }

  char *token = strtok(line, " \t,");
  while (token) {
    if (count >= capacity) {
      capacity *= 2;
      char **new_tokens = (char **)realloc(tokens, capacity * sizeof(char *));
      
      if (!new_tokens) {
        perror("malloc");
        fprintf(stderr, "ERROR: Memory allocation failed for tokens\n");
        free_tokens(tokens, count);
        return 0;
      }

      tokens = new_tokens;
    }

    tokens[count] = (char *)malloc(strlen(token) + 1);
    if (!(tokens[count])) {
      perror("malloc");
      fprintf(stderr, "ERROR: Memory allocation failed for token\n");
      free_tokens(tokens, count);
      return 0;
    }
    strcpy(tokens[count], token);
    count++;
    token = strtok(NULL, " \t,");
  }

  *token_count = count;
  *result = tokens;
  return 1;
}

int assemble_ADD(char **tokens, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int SR1 = atoi(tokens[2] + 1);
  uint16_t instr = 0x1000 | (DR << 9) | (SR1 << 6);

  if (tokens[3][0] == 'R') {
    int SR2 = atoi(tokens[3] + 1);
    instr |= SR2;
  } else {
    instr |= 0x20;
    int imm = 0;

    if ((!parse_number(tokens[3], &imm)) || (imm < -16) || (imm > 15)) {
      fprintf(stderr, "ERROR: Immediate out of range for ADD\n");
      return 0;
    }

    instr |= (imm & 0x1F);
  }

  *result = instr;
  return 1;
}

int assemble_AND(char **tokens, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int SR1 = atoi(tokens[2] + 1);
  uint16_t instr = 0x5000 | (DR << 9) | (SR1 << 6);
  
  if (tokens[3][0] == 'R' || tokens[3][0] == 'r') {
    int SR2 = atoi(tokens[3] + 1);
    instr |= SR2;
  } else {
    instr |= 0x20;
    int imm;

    if ((!parse_number(tokens[3], &imm)) || imm < -16 || imm > 15) {
      fprintf(stderr, "ERROR: Immediate out of range for AND\n");
      return 0;
    }

    instr |= (imm & 0x1F);
  }

  *result = instr;
  return 1;
}

int assemble_NOT(char **tokens, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int SR = atoi(tokens[2] + 1);
  *result = 0x9000 | (DR << 9) | (SR << 6) | 0x3F;
  return 1;
}

int assemble_LD(char **tokens, int current_pc, lc3a_label_list *label_list, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int value, offset;
  
  if (parse_number(tokens[2], &value)) {
    offset = value;
  } else {
    int status = lookup_label(label_list, tokens[2], &value);

    if (!status) {
      return 0;
    }

    offset = value - (current_pc + 1);
  }
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: Offset %d out of range for LD\n", offset);
    return 0;
  }

  *result = 0x2000 | (DR << 9) | (offset & 0x1FF);

  return 1;
}

int assemble_LDI(char **tokens, int current_pc, lc3a_label_list *label_list, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int value, offset;
  
  if (parse_number(tokens[2], &value)) {
    offset = value;
  } else {
    int status = lookup_label(label_list, tokens[2], &value);

    if (!status) {
      return 0;
    }

    offset = value - (current_pc + 1);
  }
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: Offset %d out of range for LDI\n", offset);
    return 0;
  }

  *result = 0xA000 | (DR << 9) | (offset & 0x1FF);
  return 1;
}

int assemble_LEA(char **tokens, int current_pc, lc3a_label_list *label_list, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int value = 0;
  int status = lookup_label(label_list, tokens[2], &value);

  if (!status) {
    return 0;
  }

  int offset = value - (current_pc + 1);
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: Offset %d out of range for LEA\n", offset);
    return 0;
  }

  *result = 0xE000 | (DR << 9) | (offset & 0x1FF);
  return 1;
}

int assemble_LDR(char **tokens, uint16_t *result) {
  int DR = atoi(tokens[1] + 1);
  int BaseR = atoi(tokens[2] + 1);
  int offset;
  
  if ((!parse_number(tokens[3], &offset)) || offset < -32 || offset > 31) {
    fprintf(stderr, "ERROR: Invalid offset for LDR\n");
    return 0;
  }

  *result = 0x6000 | (DR << 9) | (BaseR << 6) | (offset & 0x3F);
  return 1;
}

int assemble_ST(char **tokens, int current_pc, lc3a_label_list *label_list, uint16_t *result) {
  int SR = atoi(tokens[1] + 1);
  int value = 0, offset = 0;
  
  if (parse_number(tokens[2], &value)) {
    offset = value;
  } else {
    int status = lookup_label(label_list, tokens[2], &value);
    
    if (!status) {
      return 0;
    }

    offset = value - (current_pc + 1);
  }
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: Offset %d out of range for ST\n", offset);
    return 0;
  }

  *result = 0x3000 | (SR << 9) | (offset & 0x1FF);
  return 1;
}

int assemble_STI(char **tokens, int current_pc, lc3a_label_list *label_list, uint16_t *result) {
  int SR = atoi(tokens[1] + 1);
  int value = 0, offset = 0;
  
  if (parse_number(tokens[2], &value)) {
    offset = value;
  } else {
    int status = lookup_label(label_list, tokens[2], &value);

    if (!status) {
      return 0;
    }

    offset = value - (current_pc + 1);
  }
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: Offset %d out of range for STI\n", offset);
    return 0;
  }

  *result = 0xB000 | (SR << 9) | (offset & 0x1FF);
  return 1;
}

int assemble_STR(char **tokens, uint16_t *result) {
  int SR = atoi(tokens[1] + 1);
  int BaseR = atoi(tokens[2] + 1);
  int offset;
  
  if ((!parse_number(tokens[3], &offset)) || offset < -32 || offset > 31) {
    fprintf(stderr, "ERROR: Offset out of range for STR\n");
    return 0;
  }

  *result = 0x7000 | (SR << 9) | (BaseR << 6) | (offset & 0x3F);
  return 1;
}

int assemble_BR(char **tokens, int current_pc, const char *mnemonic, lc3a_label_list *label_list, uint16_t *result) {
  uint16_t instr = 0;
  int n = 0, z = 0, p = 0;
  
  if (strlen(mnemonic) == 2) {  // BR (unconditional)
    n = z = p = 1;
  } else {
    for (int i = 2; mnemonic[i]; i++) {
      char c = tolower(mnemonic[i]);
      if (c == 'n') {
        n = 1;
      } else if (c == 'z') {
        z = 1;
      } else if (c == 'p') {
        p = 1;
      } else {
        fprintf(stderr, "ERROR: Invalid branch condition in mnemonic %s\n", mnemonic);
        return 0;
      }
    }
  }
  
  instr |= (n << 11) | (z << 10) | (p << 9);
  
  int label_addr = 0;
  int status = lookup_label(label_list, tokens[0], &label_addr);

  if (!status) {
    return 0;
  }

  int offset = label_addr - (current_pc + 1);
  
  if (offset < -256 || offset > 255) {
    fprintf(stderr, "ERROR: BR offset %d out of range\n", offset);
    return 0;
  }
  
  instr |= (offset & 0x1FF);
  *result = instr;
  return 1;
}

int is_opcode(const char *str) {
    const char *opcodes[] = {"ADD", "AND", "LD", "LDI", "LDR", "LEA", "ST", "STI", "STR", 
                             "BR", "BRnzp", "BRnz", "BRzp", "BRn", "BRz", "BRp", "NOT", 
                             "HALT", "YIELD", "BRK", NULL};
    for (int i = 0; opcodes[i]; i++) {
      if (strcasecmp(str, opcodes[i]) == 0) {
        return 1;
      }
    }

    return 0;
}

// Assemble a single line
int assemble_line(char *line, int current_pc, int preassemble, lc3a_mach_code_list **machine_code, lc3a_label_list *label_list) {
  char *line_copy = strdup(line);
  if (!line_copy) {
    perror("malloc");
    fprintf(stderr, "ERROR: Memory allocation failed\n");
    return -1;
  }

  int token_count = 0;
  char **tokens = NULL;
  int status = tokenize(line_copy, &token_count, &tokens);

  if (!status) {
    free(line_copy);
    return -1;
  }

  free(line_copy);
  
  if ((token_count == 0) || (tokens[0][0] == ';')) {
    free_tokens(tokens, token_count);
    return 0;
  }

  uint16_t word = 0;
  if (strcmp(tokens[0], "ADD") == 0) {
    if (!preassemble) {
      status = assemble_ADD(tokens, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "AND") == 0) {
    if (!preassemble) {
      status = assemble_AND(tokens, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "NOT") == 0) {
    if (!preassemble) {
      status = assemble_NOT(tokens, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "LD") == 0) {
    if (!preassemble) {
      status = assemble_LD(tokens, current_pc, label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "LDI") == 0) {
    if (!preassemble) {
      status = assemble_LDI(tokens, current_pc, label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "LDR") == 0) {
    if (!preassemble) {
      status = assemble_LDR(tokens, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "LEA") == 0) {
    if (!preassemble) {
      status = assemble_LEA(tokens, current_pc, label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "ST") == 0) {
    if (!preassemble) {
      status = assemble_ST(tokens, current_pc, label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "STI") == 0) {
    if (!preassemble) {
      status = assemble_STI(tokens, current_pc, label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "STR") == 0) {
    if (!preassemble) {
      status = assemble_STR(tokens, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strncmp(tokens[0], "BR", 2) == 0 && strcmp(tokens[0], "BRK") != 0) {
    if (!preassemble) {
      status = assemble_BR(tokens + 1, current_pc, tokens[0], label_list, &word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }

      status = machine_code_add(machine_code, word);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "HALT") == 0) {
    if (!preassemble) {
      status = machine_code_add(machine_code, 0xF025);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "YIELD") == 0) {
    if (!preassemble) {
      status = machine_code_add(machine_code, 0xF028);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  } else if (strcmp(tokens[0], "BRK") == 0) {
    if (!preassemble) {
      status = machine_code_add(machine_code, 0xF029);
      if (!status) {
        free_tokens(tokens, token_count);
        return -1;
      }
    }
    free_tokens(tokens, token_count);
    return 1;
  }
  
  fprintf(stderr, "ERROR: Unsupported opcode: %s\n", tokens[0]);
  free_tokens(tokens, token_count);
  return -1;
}

int assemble(const char *input_file) {
  lc3a_label_list *label_list = NULL;
  lc3a_line_list *line_list = NULL;
  lc3a_mach_code_list *machine_code = NULL;
  int orig = 0;
  int status = 0;
  
  FILE *f = fopen(input_file, "r");
  if (!f) {
    fprintf(stderr, "ERROR: Cannot open file %s\n", input_file);
    return EXIT_FAILURE;
  }
  
  // Read all lines
  char buffer[MAX_LINE_LENGTH];
  while (fgets(buffer, sizeof(buffer), f)) {
    trim(buffer);
    if (strlen(buffer) > 0) {
      status = line_list_add(&line_list, buffer);

      if (!status) {
        line_list_free(line_list);
        return EXIT_FAILURE;
      }
    }
  }
  fclose(f);
  
  // PASS 1: Locate .ORIG and record label addresses
  int pc = 0;
  int started = 0;
  lc3a_line_list *non_label_lines = NULL;
  
  lc3a_line_list *current_line = line_list;
  while (current_line) {
    if (current_line->line_data[0] == ';' || strlen(current_line->line_data) == 0) {
      current_line = current_line->next;
      continue;
    }
    
    int token_count = 0;
    char *line_copy = strdup(current_line->line_data);
    char **tokens = NULL;
    status = tokenize(line_copy, &token_count, &tokens);

    if (!status) {
      free(line_copy);
      line_list_free(line_list);
      line_list_free(non_label_lines);
      label_list_free(label_list);
      machine_code_free(machine_code);
      return EXIT_FAILURE;
    }
    
    if (token_count > 0 && tokens[0][0] == '.') {
      if (strcmp(tokens[0], ".ORIG") == 0) {
        if (token_count < 2) {
          fprintf(stderr, "ERROR: .ORIG missing operand\n");
          free(line_copy);
          line_copy = NULL;
          free_tokens(tokens, token_count);
          line_list_free(line_list);
          line_list_free(non_label_lines);
          label_list_free(label_list);
          machine_code_free(machine_code);
          return EXIT_FAILURE;
        }
        if (!parse_number(tokens[1], &orig)) {
          fprintf(stderr, "ERROR: Invalid .ORIG operand\n");
          free(line_copy);
          free_tokens(tokens, token_count);
          line_list_free(line_list);
          line_list_free(non_label_lines);
          label_list_free(label_list);
          machine_code_free(machine_code);
          return EXIT_FAILURE;
        }
        pc = orig;
        started = 1;
      }
      free(line_copy);
      free_tokens(tokens, token_count);
      current_line = current_line->next;
      continue;
    }
    
    if (!started) {
      fprintf(stderr, "ERROR: Missing .ORIG before instructions\n");
      free(line_copy);
      free_tokens(tokens, token_count);
      line_list_free(line_list);
      line_list_free(non_label_lines);
      label_list_free(label_list);
      machine_code_free(machine_code);
      return EXIT_FAILURE;
    }
    
    if (token_count > 0) {
      if (!is_opcode(tokens[0])) {
        // It is a label
        status = label_list_add(&label_list, tokens[0], pc);
        if (!status) {
          free(line_copy);
          free_tokens(tokens, token_count);
          line_list_free(line_list);
          line_list_free(non_label_lines);
          label_list_free(label_list);
          machine_code_free(machine_code);
          return EXIT_FAILURE;
        }
      } else {
        // It is an instruction
        status = line_list_add(&non_label_lines, current_line->line_data);
        if (!status) {
          free(line_copy);
          free_tokens(tokens, token_count);
          line_list_free(line_list);
          line_list_free(non_label_lines);
          label_list_free(label_list);
          machine_code_free(machine_code);
          return EXIT_FAILURE;
        }
        
        char *temp_copy = strdup(current_line->line_data);

        int pc_inc = assemble_line(temp_copy, pc, 1, &machine_code, label_list);
        if (pc_inc != -1) {
          pc += pc_inc;
        } else {
          fprintf(stderr, "ERROR: .asm file could not be traversed\n");
          free(temp_copy);
          free(line_copy);
          free_tokens(tokens, token_count);
          line_list_free(line_list);
          line_list_free(non_label_lines);
          label_list_free(label_list);
          machine_code_free(machine_code);
          return EXIT_FAILURE;
        }
        
        free(temp_copy);
      }
    }
    
    free(line_copy);
    free_tokens(tokens, token_count);
    current_line = current_line->next;
  }
  
  // PASS 2: Assemble instructions
  pc = orig;
  lc3a_line_list *current_instr = non_label_lines;
  while (current_instr) {
    char *line_copy = strdup(current_instr->line_data);

    int pc_inc = assemble_line(line_copy, pc, 0, &machine_code, label_list);
    if (pc_inc != -1) {
      pc += pc_inc;
    } else {
      fprintf(stderr, "ERROR: PC could not be updated properly\n");
      free(line_copy);
      line_list_free(line_list);
      line_list_free(non_label_lines);
      label_list_free(label_list);
      machine_code_free(machine_code);
      return EXIT_FAILURE;
    }

    free(line_copy);
    current_instr = current_instr->next;
  }
  
  status = machine_code_add(&machine_code, 0x4000);

  if (!status) {
    line_list_free(non_label_lines);
    label_list_free(label_list);
    line_list_free(line_list);
    machine_code_free(machine_code);
    return EXIT_FAILURE;
  }
  
  char *output_file = (char *)malloc(strlen(input_file) + 20);
  if (!output_file) {
    perror("malloc");
    line_list_free(line_list);
    line_list_free(non_label_lines);
    label_list_free(label_list);
    machine_code_free(machine_code);
    return EXIT_FAILURE;
  }
  strcpy(output_file, input_file);
  char *dot = strrchr(output_file, '.');
  if (dot) {
    *dot = '\0';
  }
  strcat(output_file, "_code.obj");
  
  FILE *out = fopen(output_file, "wb");
  if (!out) {
    fprintf(stderr, "ERROR: Cannot create output file %s\n", output_file);
    free(output_file);
    line_list_free(line_list);
    line_list_free(non_label_lines);
    label_list_free(label_list);
    machine_code_free(machine_code);
    return EXIT_FAILURE;
  }
  
  lc3a_mach_code_list *current_code = machine_code;
  while (current_code) {
    fwrite(current_code->mach_code, sizeof(uint16_t), 1, out);
    current_code = current_code->next;
  }
  fclose(out);
  
  free(output_file);
  line_list_free(non_label_lines);
  label_list_free(label_list);
  line_list_free(line_list);
  machine_code_free(machine_code);

  return EXIT_SUCCESS;
}

int main(int argc, char **argv) {
  if (argc != 2) {
    printf("Usage: ./lc3c <filename>.lc3\n");
    
    exit(EXIT_FAILURE);
  }

  const char *filename = argv[1];
  const char *ext_lc3 = strrchr(filename, '.');
  if ((!ext_lc3) || (strcmp(ext_lc3, ".lc3") != 0)) {
    if (ext_lc3) {
      fprintf(stderr, "ERROR: Incorrect file extension: %s\n", ext_lc3);
    } else {
      fprintf(stderr, "ERROR: Incorrect file extension for file: %s\n", filename);
    }
    
    exit(EXIT_FAILURE);
  }

  const char *dot = strrchr(filename, '.');
  size_t base_len = (size_t)(dot - filename);
  char *outname_asm = (char *)malloc(base_len + 5);

  if (!outname_asm) {
    perror("malloc");
    
    exit(EXIT_FAILURE);
  }

  memcpy(outname_asm, filename, base_len);
  memcpy(outname_asm + base_len, ".asm", 5);


  int status = EXIT_SUCCESS;
  status = compile_file(filename, outname_asm);

  if (status == EXIT_FAILURE) {
    return status;
  }

  const char *ext_asm = strrchr(outname_asm, '.');
  if ((!ext_asm) || (strcmp(ext_asm, ".asm") != 0)) {
    if (ext_asm) {
      fprintf(stderr, "ERROR: Incorrect file extension: %s\n", ext_asm);
    } else {
      fprintf(stderr, "ERROR: Incorrect file extension for file: %s\n", filename);
    }

    return EXIT_FAILURE;
  }

  status = assemble(outname_asm);
  
  if (status == EXIT_FAILURE) {
    fprintf(stderr, "ERROR: %s could not be assembled\n", outname_asm);
  }
  else {
    printf("%s is successfully assembled\n", outname_asm);
  }

  return status;
}